;; NullDC4Wii - Game Presets Configuration
;; Place this file at: sd:/discs/game_presets.cfg
;;
;; HOW IT WORKS:
;;   Each [keyword] is matched case-insensitively as a substring of the disc filename.
;;   A section may list several aliases on one line — [name1][name2][name3] —
;;   and applies if ANY of them matches (e.g. [streetfighter32][doubleimpact]).
;;   The first alias is the name shown in the options menu. Up to 8 per section.
;;   The FIRST matching rule wins — put more specific entries BEFORE general ones.
;;   Only fields you list are changed; everything else keeps the user's UI selection.
;;   The options menu opens AFTER the preset is applied, so users can still override.
;;
;;   Angle brackets are the SAME alias match as square brackets, but ONLY
;;   apply on real Wii U hardware running vWii — e.g. <segatetris><sega tetris>.
;;   [square] and <angle> groups may be mixed freely on one line, in any order;
;;   if ANY group on the line uses <angle> brackets, the whole section is
;;   Wii-U-gated. A real Wii just falls through to a plain [square] section
;;   for the same game below it. Put the <angle> section FIRST, same
;;   "more specific before general" rule:
;;
;;     <segatetris><sega tetris>        ; Wii U
;;     accuracy=accurate
;;
;;     [segatetris][sega tetris]        ; Wii (fallback for everyone else)
;;     accuracy=fast
;;
;; SETTINGS REFERENCE:
;;   accuracy    = fast | balanced | accurate
;;   graphics    = low | normal   (TEXTURE FILTER ONLY: low = GX_NEAR point sampling,
;;                 best for 240p games whose art already matches the output resolution;
;;                 normal = GX_LINEAR bilinear. The old high/extra levels were nothing but
;;                 normal plus a fixed lod_bias/bias-clamp/aniso bundle - those are the three
;;                 separate keys below now. Old configs saying high/extra still load: they map
;;                 to normal and log a line pointing at gx=/lod_bias=/aniso=)
;;   gx          = on | off   (GX_ENABLE/GX_DISABLE of GX_InitTexObjLOD's biasclamp + edgelod
;;                 pair - the only two GX_DISABLE/GX_ENABLE arguments that call takes.
;;                 biasclamp stops lod_bias from pushing a minified texel past the point where
;;                 its footprint no longer covers the pixel (blur/shimmer on steep surfaces);
;;                 edgelod computes the LOD from adjacent instead of diagonal texels.
;;                 off by default = legacy. aniso below forces edgelod on by itself, since
;;                 libogc requires it whenever maxaniso > 1. gx_enable/gx_disable are
;;                 accepted spellings of on/off here)
;;   lod_bias    = -1 | -0.75 | -0.5 | 0.0 | 0.5   (texture LOD bias added to the computed LOD
;;                 before the mip/filter decision: negative sharpens - samples a larger level
;;                 than the footprint asks for - positive blurs. 0.0 (default) is the hardware
;;                 default and a true no-op)
;;   aniso       = 0x | 2x | 4x   (anisotropic filtering: the TX unit iterates its square
;;                 filter along the axis of anisotropy, one extra filter cycle per level -
;;                 sharper textures on surfaces seen at a steep angle, at fill-rate cost.
;;                 Hollywood only implements 1x/2x/4x - there is no 8x mode on this GPU; an
;;                 old aniso=8x line loads clamped to 4x.
;;                 REQUIRES mipmap=fast or mipmap=trilinear: aniso is only iterated when the
;;                 min filter is GX_LIN_MIP_LIN, which needs a generated mip chain, so it does
;;                 NOTHING in a game left at mipmap=off (the default). Asking for 2x/4x does
;;                 promote an already-mipmapped texture to trilinear on its own.
;;                 0x (default) = off)
;;   ratio       = original | fullscreen | auto   (auto = match Wii system 4:3/16:9 aspect)
;;   adv_alpha   = on | off
;;   frameskip   = 0 | 1 | 2 | auto | auto_max
;;                 auto     = drop renders while behind real time, min 1 render in 4
;;                 auto_max = same, but up to 9 skips in a row (speed over framerate)
;;                 both hold emulated time at 100% speed; the FPS counter shows the
;;                 RENDER rate, so a low FPS with SPEED at 100% is working as intended
;;   tex_cache   = very_fast | very_fast_plus | fast | normal | quality
;;                 very_fast      = original skmp direct-slot cache: fastest, but slots are
;;                                  derived from the texture address with no size accounting,
;;                                  so a texture that decodes bigger than its slot overruns
;;                                  the next one (VQ is the worst case: ~4x). That overrun is
;;                                  the "very_fast need to be re-tested (very_fast+ and vq_cmbr=on)/buggy" corruption.
;;                 very_fast_plus = very_fast's address-derived texture slots -- which never
;;                                  evict anything, and measured 0 decodes/frame against
;;                                  normal's 73 on the same Buggy Heat scene -- with the
;;                                  textures that would overrun such a slot routed into a
;;                                  properly sized arena instead. Plus three fixes:
;;                                  (1) a stride-selected texture decodes 512 pixels wide, not
;;                                      the width it declares, so its slot was up to 16x too
;;                                      small and the decode ran off the end of it. In the
;;                                      per-frame quality arena that overrun is harmless (it
;;                                      lands on space not handed out yet); in the persistent
;;                                      fast/normal arena it permanently wrecks a neighbouring
;;                                      cached texture, which then re-decodes forever and can
;;                                      overrun the NEXT one. That is why 2D menus look wrong
;;                                      AND run slowly on fast/normal but are fine on
;;                                      very_fast and quality.
;;                                  (2) stride surfaces are validated by hashing 8 words of
;;                                      their contents. very_fast and quality never cache them
;;                                      at all (correct, but every draw re-converts 512*h
;;                                      texels); hashing lets static menu art cache while
;;                                      animated art still refreshes.
;;                                  (3) the cache sentinel is written at the un-mipped address.
;;                                      fast/normal write it past the mip levels, where the hit
;;                                      test never looks, so no mipmapped texture ever caches
;;                                      and the stomp lands in the visible full-size level.
;;                                  Try this first when very_fast is the only fast option but
;;                                  shows corruption.
;;   ppz_write   = on | off   (PVR Polygon-List Z write fix — Test Drive 6, Demolition Racer)
;;   vq_cmpr     = on | off   (VQ textures -> GX CMPR/DXT1 at 4 bits/texel instead of
;;                 16bpp. Pairs with tex_cache=very_fast_plus, and helps plain very_fast
;;                 too, which uses the same slots: at that size a VQ texture
;;                 fits its address-derived slot, so it stops overrunning the texture
;;                 next to it -- the classic very_fast VQ corruption -- and stops needing
;;                 the overflow arena. Second lossy pass on top of VQ, so smooth
;;                 gradients band; 565 VQ only. Default off.)
;;   4bpp        = i4_stub | 4bpp_optimized | ci4_fast | ci4_normal | rgb565
;;   8bpp        = i8_stub | 8bpp_optimized | ci8_fast | ci8_normal | rgb565
;;   players     = 1 | 2 | 3 | 4
;;   controller  = standard | lightgun | maracas | keyboard | fishingrod
;;   framebuffer_2d = on | off   (2D Framebuffer rendering — try for 2D games)
;;   fmv_format     = cmpr | rgba8 | rgb565   (FMV/YUV422 texture upload format)
;;   jojo_fix       = on | off   (gxRend.cpp TLUT-reupload-skip + CACHE_FAST PalSelect-masking,
;;                             tuned for Jojo's Bizarre Adventure — off by default elsewhere)
;;   decal_alpha    = on | off   (ShadInstr==2 DecalAlpha shading fix — GX_DECAL for correct
;;                             transparency vs legacy GX_MODULATE which is faster but wrong)
;;   speed_limiter  = on | off   (caps emulation at real-hardware 100% speed; off by default
;;                             since the SH4 thread normally runs uncapped — use this for
;;                             games that visibly run too fast on light frames. It is the
;;                             'too fast' half of the same schedule frameskip=auto uses for
;;                             its 'too slow' half, so pair the two to pin a game at 100%)
;;   render_delay   = on | off   (hardware-like HOLLY IRQ delays. Games that pace
;;                             their main loop off the TA list-complete /
;;                             render-done interrupts break when those fire
;;                             instantly: speed reads >100% while only a few
;;                             frames per second render (Marvel vs Capcom 2,
;;                             "130% / 7 FPS"). on delays each list-complete
;;                             IRQ by 200 SH4 cycles and staggers render-done
;;                             ISP/TSP/Video at 800k/850k/900k cycles after
;;                             STARTRENDER, like real hardware. Default off.)
;;   jit_sbp        = 0 | 1 | 2  (JIT Stale Block Protection. Gates all defenses
;;                             against executing stale/self-modified SH4
;;                             translations: the boot-entry cache flush at the
;;                             IP.BIN -> game-binary transition, the runtime
;;                             block-check guard (re-checks the source byte at
;;                             block entry, 1 = known self-modifying addresses
;;                             like DOA2LE/Shenmue 1/2, 2 = every RAM block —
;;                             slow, diagnosis only), and the cache clear on a
;;                             guard failure. Default 1 (known). 0 reproduces
;;                             the legacy zero-protection behavior, for A/B
;;                             comparison only.)
;;   dma_fix        = on | off  (bundles the ch2/PVR/Sort/AICA-G2 DMA
;;                             correctness fixes ported from the verified-
;;                             working NullDC PSP port: correct CHCR TE/DE
;;                             writeback, no SB_C2DSTAT clobber, no bogus
;;                             PVR-DMA alignment check, the Sort-DMA link-
;;                             sentinel fix for WinCE games, and deferred
;;                             AICA G2-DMA completion + SB_E2ST. Default on.
;;                             off reproduces the legacy pre-fix behavior,
;;                             for A/B comparison only.)
;;   vertex_color = on | off  (real PVR Intensity/Gouraud shading: multiplies each
;;                             vertex's intensity by the polygon's FaceColor instead of
;;                             rendering flat grayscale — off by default elsewhere;
;;                             Crazy Taxi needs this for its HUD arrow/dollar sign color)
;;   blend_mode      = on | off   (per-polygon TSP SrcInstr/DstInstr blending for the
;;                             translucent list — on applies the polygon's actual blend
;;                             factors each frame (correct); off skips the override and
;;                             uses the GX default (legacy, but wrong for RE3))
;;   rgb565_opaque_alpha = on | off  (force vertex alpha opaque for RGB565 (fmt1) textures.
;;                             on (default): both ARGB1555 (fmt0) and RGB565 (fmt1) force
;;                             vertex alpha to 0xFF, matching existing behavior.
;;                             off: only ARGB1555 (fmt0) is forced opaque; RGB565 (fmt1)
;;                             uses vertex alpha as-is, allowing translucent RGB565 polys)
;;   fps_boost       = on | off  (only takes effect when blend_mode=on. on skips the
;;                             alpha-test/ZCompLoc pass for every polygon outside the
;;                             translucent list, gaining a couple of FPS (e.g. Castlevania)
;;                             at the cost of incorrect alpha on some opaque/punch-through
;;                             polys. Default off (correct alpha).)
;;   trans_sort      = on | off  (translucent depth sort. Real PVR autosorts translucent
;;                             pixels in hardware; on sorts the translucent strips
;;                             back-to-front before drawing, fixing wrong overlaps in
;;                             alpha-heavy scenes at some CPU cost per frame.
;;                             Default off (legacy submission-order draw).)
;;   autosort        = 0 | 1 | 2 | 3 | 4  (REAL per-pixel PVR autosort via GX depth
;;                             peeling — stronger than trans_sort's per-strip
;;                             painter sort, for intersecting/interleaved
;;                             translucent geometry. Value is the max translucent
;;                             depth LAYERS composited per pixel; each layer costs
;;                             ~2 extra walks of the translucent geometry + 2 EFB Z
;;                             copies, so use 2 or 3 only where needed. Overrides
;;                             trans_sort; layer_sort overrides it. Best paired
;;                             with punch_through=on. 0 (default): off.)
;;   punch_through   = on | off  (punch-through list fix. on (default): draws lists
;;                             in OP -> PT -> TR order with the PT list alpha-tested
;;                             against PT_ALPHA_REF and blending off, like real PVR.
;;                             off (legacy): PT polys drawn last, in the translucent
;;                             blend state.)
;;   offset_color    = on | off  (offset/specular color. on: textured polys render as
;;                             PIX = base*tex + offset like real PVR (specular
;;                             highlights on cars/water), costing 4 bytes/vertex of
;;                             FIFO + a second TEV stage on offset polys. off
;;                             (default): offset color dropped, legacy.)
;;   render_to_texture = on | off | overlay | keep  (render-to-texture. Frames whose write
;;                             address (FB_W_SOF1) has bit 24 set target the 64-bit
;;                             texture area — mirrors, TV screens, sniper scopes,
;;                             some menu effects. on:
;;                             renders them and copies the EFB back into emulated
;;                             VRAM so the game can bind the result as a texture, at
;;                             the cost of an EFB copy + CPU convert per RTT frame.
;;                             keep: "on" PLUS the render does not consume the TA
;;                             list. On real hardware a render never does — the
;;                             ISP/TSP just walk the tile arrays, which stay valid
;;                             until TA_LIST_INIT — so a game may render ONE
;;                             accumulated list twice with different write
;;                             addresses and clip windows. Silent Scope's sniper
;;                             scope is built that way: one list holds the scope
;;                             disc plus the 24x-magnified world, it is rendered
;;                             first into a 256x256 texture (clip 0..255), then the
;;                             ordinary world is appended and the WHOLE list is
;;                             rendered to the screen. With plain "on" the reset
;;                             after the texture pass throws the first half away
;;                             and the scope disc never appears. Pair with
;;                             split_screen=on so the magnified half stays inside
;;                             its user tile clip instead of covering the screen.
;;                             overlay: does NOT resolve a texture — instead the
;;                             pass's geometry is carried into the next display
;;                             frame and drawn last, flat on top (parked on the near
;;                             plane, GX_ALWAYS, no Z-write), with its own OP/TR/PT
;;                             list boundaries replayed. Use it when "on" gives
;;                             nothing and the pass is really an overlay the game
;;                             composites itself — Silent Scope's sniper crosshair.
;;                             off (default): those frames are dropped — but their
;;                             geometry is left in the vertex buffers, so it leaks
;;                             into the next frame, drawn first, misclassified as
;;                             opaque, and mostly overdrawn by the scene. That leak
;;                             is why some overlays half-show with RTT off; overlay
;;                             is the same idea done deliberately.)
;;   yuv_stride = off | auto | always | texctl  (how the REAL per-row pixel pitch of a
;;                             YUV422 source is decided. A YUV422 texture is
;;                             declared at a power-of-two size, but an FMV frame
;;                             written by the YUV converter is packed at the
;;                             video's native width (e.g. 320) with no padding,
;;                             so decoding it at the declared width slices the
;;                             picture into repeated strips.
;;                             auto (default): use the real pitch only for a
;;                             texture the YUV converter actually wrote, which
;;                             is what fixed Bomberman's intro, Test Drive 6 and
;;                             Dino Crisis.
;;                             always: the old behaviour - take the pitch from
;;                             the TA_YUV_TEX_CTRL register for EVERY YUV422
;;                             texture. That register belongs to the converter
;;                             and says nothing about a texture the game
;;                             uploaded itself: a game that never programs it
;;                             leaves it at 0, which decodes as 16x16 and blanks
;;                             the texture - Soul Calibur's character select went
;;                             mostly black.
;;                             texctl: auto, plus the actual hardware rule as a
;;                             fallback for the surfaces auto's converter
;;                             tracking never saw (it only remembers 4 base
;;                             addresses, so a game cycling more FMV buffers
;;                             than that falls through). TCW.StrideSel means the
;;                             rows are packed at TEXT_CONTROL[4:0] x 32 texels;
;;                             that is a SOURCE PITCH and is applied as one -
;;                             the declared width is left alone, because every
;;                             UV in the strip is relative to it and rewriting
;;                             it re-slices the picture rather than unslicing
;;                             it. texctl also fixes the planar RGB path, which
;;                             still guesses a blind 512 under every other mode.
;;                             If a movie is still messy under auto, try texctl
;;                             before always.
;;                             off: always decode at the declared width.)
;;   yuv_twiddle_fix = on | off  (fixes TWIDDLED YUV422 textures — static YUV
;;                             artwork, NOT movie playback. The legacy decoder
;;                             swapped the two u16 halves of each source u32 and
;;                             read luma from the low byte / chroma from the high
;;                             byte, so luma and chroma traded places and the
;;                             picture collapsed onto a green<->magenta duotone
;;                             with 1-column striping. Turn on for any game whose
;;                             YUV artwork comes out green — Virtua Fighter 3tb's
;;                             "FIRST MATCH" loading screen is the reference case.
;;                             Real FMV is planar and is not affected either way.
;;                             Default off.)
;;   seam_fix        = on | off  (half-texel UV inset via a per-texture GX matrix.
;;                             Stops GX_LINEAR from sampling past a sprite's own
;;                             texels, killing the thin black seam line between 2D
;;                             tiles/sprites without dropping to GX_NEAR — keeps
;;                             wrap/tiling intact. Default off.)
;;   fog             = on | off  (per-polygon PVR2 fog. Honours each polygon's
;;                             TSP.FogCtrl: look-up table (FOG_TABLE/FOG_DENSITY,
;;                             colour FOG_COL_RAM), per-vertex (coefficient = the
;;                             vertex's offset-colour alpha, colour FOG_COL_VERT),
;;                             no fog, and LUT mode 2. The coefficient is worked
;;                             out per vertex on the CPU and blended by one extra
;;                             TEV stage, so foggy games get their distance haze
;;                             back instead of hard geometry that pops in at the
;;                             draw distance — racers and outdoor games above all.
;;                             Costs 4 bytes/vertex and one TEV stage on fogged
;;                             polygons only. Default off.)
;;   x_scaler        = on | off  (PVR horizontal X-scaler support. A few games
;;                             (Omicron The Nomad Soul, Wacky Races) render the
;;                             scene 1280 pixels wide and let the PVR video
;;                             scaler halve it 2:1 on framebuffer write
;;                             (horizontal SSAA). on widens the projected
;;                             canvas to 1280 so the whole scene shows; off
;;                             (default, legacy) keeps a 640 canvas, showing
;;                             only the LEFT HALF of the image in those games.)
;;   y_scaler        = on | off  (PVR vertical Y-scaler support - the other half
;;                             of the same register x_scaler reads.
;;                             SCALER_CTL.vscalefactor is 6.10 fixed point
;;                             (0x400 = 1.0): above 1.0 the CORE renders the
;;                             scene that many times TALLER and the video
;;                             scaler shrinks it back on framebuffer write
;;                             (vertical SSAA / flicker filter), below 1.0 it
;;                             renders shorter and the scaler stretches. on
;;                             scales the projected canvas height by that
;;                             factor so the whole scene shows; off (default,
;;                             legacy) ignores it, showing only a SLICE of the
;;                             image in those games.)
;;   h_scaler        = on | off  (PVR horizontal H-scaler at video output:
;;                             VO_CONTROL.pixel_double. In the low-res video
;;                             modes the framebuffer holds a HALF-width (320)
;;                             image and the video DAC emits every pixel twice
;;                             to fill the 640-pixel line, so the scene is
;;                             drawn in a 320-wide screen space. on halves the
;;                             canvas to match; off (default, legacy) projects
;;                             it into a 640 canvas where it fills only the
;;                             LEFT HALF. Register-driven counterpart of
;;                             canvas_width below, which stays the manual
;;                             override for games that render narrow WITHOUT
;;                             setting the bit - an explicit canvas_width
;;                             wins over this.)
;;   canvas_width    = 0 | 320..1280  (forced canvas width for 240p modes.
;;                             Some low-res games draw their scene narrower
;;                             than 640 and pad the rest with black filler,
;;                             so neither registers nor measurement reveal
;;                             the real width. This forces it, only while
;;                             the video mode is non-interlaced NTSC/PAL
;;                             (240p) — interlaced boot/logo screens keep
;;                             the 640 canvas. Street Fighter III / Double
;;                             Impact: 384 (CPS3 arcade width). 0 (default):
;;                             off.)
;;   split_screen    = off | on (=tile_clip) | multipass | both  (split-screen
;;                             multiplayer. The Dreamcast has TWO ways to confine a
;;                             player's camera to its half of the screen and games
;;                             pick either one, so this key carries both.
;;                             on / tile_clip: both viewports arrive in ONE render
;;                             pass, each player's polygons carrying a PVR User Tile
;;                             Clip rect (Daytona USA, confirmed). Without it the two
;;                             cameras draw fullscreen on top of each other and you
;;                             mostly see player 1.
;;                             multipass: the game issues ONE RENDER_START PER
;;                             VIEWPORT into the same framebuffer, each restricted to
;;                             its band of it. Legacy presents every one of those as a
;;                             finished frame, so the display alternates player 1 /
;;                             player 2 / player 1 — HEAVY FLICKER. Try this whenever a
;;                             2P split screen flickers rather than showing one camera
;;                             (Le Mans 24 Hours, Demolition Racer, Magical Racing
;;                             Tour). Each partial pass is drawn into its own band of
;;                             the EFB and ONE assembled frame is presented.
;;                             both: per-poly tile clips inside multi-pass renders.
;;                             Default off (legacy).)
;;   mipmap          = off | fast | trilinear  (GX mipmap generation. off (default):
;;                             legacy base-level-only, fastest. fast: generated mip
;;                             chain sampled with nearest-mip bilinear — cuts distant
;;                             shimmer at near-zero GPU cost. trilinear: best quality
;;                             but takes 2 texture cycles/texel on Hollywood, halving
;;                             texture fill rate — e.g. -40% FPS in Test Drive 6.)
;;   fixed_depth     = 0 | 1 | 2  (fixed depth projection. 0 (default): legacy
;;                             per-vertex min/max W tracking, fits the Z range to
;;                             the scene each frame. 1 (wide): skips that tracking,
;;                             fixed planes W=[0.0001..100000] — safe everywhere,
;;                             coarser Z (more Z-fighting risk). 2 (tight): fixed
;;                             planes W=[0.1..25000] — much finer Z, but geometry
;;                             outside that range clips. UNVERIFIED on real
;;                             hardware — test per-game before relying on it.)
;;   legacy_depth    = on | off  (off by default. Reproduces the depth pipeline
;;                             exactly as it stood at commit 1bb8c27, the last
;;                             state known to render certain games correctly.
;;                             Three things differ from today and all three are
;;                             restored together: fixed planes NEAR=0.001 and
;;                             FAR=10000*1.001; vert_base clamps 1/W at 0.001
;;                             instead of 0.0001; and no per-vertex min/max
;;                             tracking or margin/HUD vertex fixups. The clamp is
;;                             the part fixed_depth cannot express, which is why
;;                             no fixed_depth value reproduces 1bb8c27.
;;                             Overrides fixed_depth.)
;;   depth_clip      = 0 | 1 | 2  (real-Wii Z-clip workaround. Dolphin never
;;                             Z-clips (it clamps), so geometry sitting on or
;;                             beyond the near/far plane — often the 2D UI,
;;                             which defines the dynamic near plane itself —
;;                             shows in Dolphin but vanishes on real hardware.
;;                             0 (default): legacy, XF clipping on, no margin.
;;                             1 (near margin): pads the near plane by 0.1% so
;;                             the nearest layer can't land exactly on it —
;;                             CONFIRMED on real Wii hardware, fixes ChuChu
;;                             Rocket and Crazy Taxi menu/intro with no
;;                             downside seen so far. 2 (no clip): disables XF
;;                             clipping entirely to fully match Dolphin —
;;                             regressed Crazy Taxi when combined with
;;                             fixed_depth=2 (tight), avoid that combo.)
;;   hud_pass        = 0 | 1 | 2  (rescue the 2D HUD that fixed_depth=tight clips.
;;                             0 (default): legacy. Any strip sitting nearer than
;;                             the projection near plane (the HUD, which parks in
;;                             front of the tight W=0.1 plane) is re-parked onto
;;                             the plane with its screen position preserved and
;;                             drawn GX_ALWAYS. 1 = overlay (no Z-write): the HUD
;;                             may be overdrawn by geometry drawn after it. 2 =
;;                             protect (Z-write at the near plane): later scene
;;                             polys fail GEQUAL over HUD pixels so it stays on
;;                             top — use this if mode 1 leaves polys in front of
;;                             the HUD; avoid on games with a large near-clipped
;;                             quad drawn early. Companion to fixed_depth=2 — a
;;                             no-op on its own (dynamic/wide ranges never clip
;;                             the HUD). Fixes the "Z-fighting gone but HUD
;;                             vanished" case in Rayman 2 / Cannon Spike.
;;                             UNVERIFIED on real hardware — test per-game.)
;;   async_render    = on | off  (async GPU present. off (default): CPU blocks in
;;                             GX_DrawDone() until the GPU finishes each frame.
;;                             on: queues the frame and returns at once — the
;;                             SH4 core emulates the next frame while the GPU
;;                             draws, and the finished frame is presented at
;;                             the start of the next one. One frame of extra
;;                             display latency; large FPS gain whenever the
;;                             GPU takes a meaningful slice of the frame.
;;                             UNVERIFIED on real hardware — test per-game.)
;;   show_fps        = on | off  (gameplay FPS/speed text overlay, drawn straight to
;;                             the XFB by the software overlay in gxRend.cpp. off
;;                             (default): no overlay. on: shows live FPS + speed%.)
;;   tmem_cache      = on | off  (persistent GPU texture cache. off (default):
;;                             invalidates the GPU's 1MB texture cache (TMEM)
;;                             every frame, re-fetching every texel from RAM.
;;                             on: invalidates only when a texture is actually
;;                             re-decoded, so unchanged textures stay cached
;;                             across frames — more texture fill rate.
;;                             UNVERIFIED on real hardware — test per-game.)
;;   cdda            = on | off  (CD audio (Red Book) music. off (default): the
;;                             AICA EXTS0 input stays silent, so games that
;;                             play music as CD audio tracks have no music.
;;                             on: streams the playing track's sectors from the
;;                             disc image (~75 reads/s) into the sound mix.
;;                             UNVERIFIED on real hardware — test per-game.)
;;   mute_pcm16      = on | off  (silence 16-bit PCM channels. off (default): all
;;                             sample formats play normally. on: any AICA channel
;;                             whose PCMS==0 (16-bit PCM) is dropped at KEY_ON — a
;;                             workaround for ChuChu Rocket's echoey/slow-motion
;;                             16-bit SFX. Also mutes 16-bit music/voices, so this
;;                             is a per-game hack, not a general fix.)
;;   vtx_alpha       = on | off  (vertex alpha on ARGB1555 surfaces.
;;                             off (default): legacy — vertex alpha is forced to
;;                             0xFF on EVERY ARGB1555 polygon whatever
;;                             TSP.UseAlpha says. That is a deliberate hack for
;;                             ARGB1555 cutout fonts (Test Drive 6) where games
;;                             leave vertex alpha at 0 and let the TEXTURE alpha
;;                             gate visibility.
;;                             on: honour TSP.UseAlpha and nothing else, i.e. the
;;                             hardware's own rule. Needed by anything that FADES
;;                             via vertex alpha on an ARGB1555 surface: that
;;                             format has only 1 bit of texture alpha, so the fade
;;                             has to live in the 8-bit vertex / sprite-header
;;                             alpha, and forcing it opaque pins the overlay on
;;                             screen forever. Fighting Vipers 2's SEGA screen is
;;                             exactly this — a full-screen fade overlay that
;;                             covered everything behind it.
;;                             Pair it with sprite_color for sprite-based fades.)
;;   list_order      = on | off  (PVR list-type render order, not TA order.
;;                             Real PVR2 sorts every polygon into a per-tile
;;                             OPAQUE / PUNCH-THROUGH / TRANSLUCENT list and
;;                             renders them OP -> PT -> TR, whatever order the
;;                             game handed the lists to the TA. This renderer
;;                             keeps one flat strip buffer walked in SUBMISSION
;;                             order, which quietly assumes the game submits its
;;                             opaque list first. A game that opens the
;;                             translucent list FIRST and its opaque list after
;;                             gets its opaque geometry drawn LAST, painting over
;;                             the whole scene.
;;                             off (default): legacy submission-order walk.
;;                             on: when the opaque list opened after the
;;                             translucent one, its range is drawn first, as the
;;                             hardware would. Bit-identical to off for every
;;                             game that submits OP first (the condition never
;;                             fires), so it is only worth setting on a game that
;;                             shows this symptom: gameplay visible for a moment,
;;                             then a big background image covering everything.
;;                             Puyo Puyo 4 / Puyo Puyo DA! does exactly that --
;;                             its two full-screen background plates go out in the
;;                             OPAQUE list after the gameplay sprites.)
;;   layer_back_tex  = 0 | <addr>[,<addr>...]  (name up to 4 textures as
;;                             BACKDROPS, not overlays. For a 2D game that submits its playfield
;;                             backdrop LATE in the translucent list, at the same
;;                             depth as the sprites already standing in it:
;;                             painter order then paints the backdrop over its
;;                             own contents, and because it is alpha-blended the
;;                             sprites do not vanish, they just go DIM.
;;                             Symptom: "the sprites are not bright enough, I can
;;                             see them behind the translucent background".
;;                             Give it the texture's VRAM address (hex or
;;                             decimal, from a [SCN] census addr= field) and its
;;                             strips sort behind every other one at that depth.
;;                             Find the address with debug_skip_tex: hide one
;;                             texture at a time and see what stops being
;;                             covered. Puyo Puyo 4 = 0x118000, its two
;;                             playfields. 0 = off (default).
;;                             A COMMA LIST names more than one, which a game
;;                             with more than one backdrop needs (a menu screen
;;                             and a gameplay screen rarely share a plate):
;;                               layer_back_tex=0x118000,0x054000
;;                             Up to 4; a bare address is still valid, it is a
;;                             list of one. This is the general, cfg-only
;;                             mechanism -- a per-game hack with hardcoded
;;                             addresses (Puyo Puyo 4's own two, matching the
;;                             example above) lives entirely in gxRend.cpp
;;                             instead, behind its own `puyo_hack` on/off
;;                             switch (also a page-6 menu row), so the SD card
;;                             never needs touching to use it. This key and
;;                             that switch both feed the same address list and
;;                             happily combine if both are set.
;;                             Implies the translucent sort. Address-keyed on
;;                             purpose: at one shared depth there is no depth to
;;                             sort on and a backdrop is indistinguishable from
;;                             an ordinary sprite by blend/format/size, so a
;;                             heuristic cannot find it -- see the long
;;                             Hokuto no Ken v1..v11 history for what guessing a
;;                             signature costs.)
;;   sprite_color    = on | off  (honour a Sprite's Base Colour.
;;                             A Sprite (PCW.ParaType 5) has no per-vertex
;;                             colour: its header carries ONE packed Base Colour
;;                             for all four corners. The renderer hardcoded white
;;                             and discarded that field, so EVERY sprite in EVERY
;;                             game drew with white vertex colour. With
;;                             TSP.ShadInstr = ModulateAlpha (colour = texture x
;;                             vertex) a black sprite then comes out white.
;;                             Found on Fighting Vipers 2's SEGA screen: the
;;                             full-screen black backdrop is a sprite, and it was
;;                             drawing as a white plate over the whole display.
;;                             on (DEFAULT): reading the field is simply
;;                             correct, and discarding it was silently wrong for
;;                             every sprite in every game. Set it off per-game
;;                             only if some title turns out to depend on the old
;;                             white -- if that becomes common this wants an
;;                             auto rule rather than a switch.)
;;   trans_zwrite    = on | off  (DEBUG ONLY. depth WRITE for the translucent
;;                             on (default): legacy — the TR list inherits the
;;                             frame-wide GEQUAL compare WITH depth write, so the
;;                             first translucent strip submitted at a near depth
;;                             stamps the Z buffer and depth-rejects every farther
;;                             translucent strip behind it. One near full-screen
;;                             quad can hide the entire rest of the list.
;;                             off: keep the depth TEST (opaque geometry still
;;                             occludes translucent geometry behind it) but drop
;;                             the write, so translucent strips composite in
;;                             submission order.
;;                             DEBUG ONLY: off HIDES the Dreamcast BIOS boot
;;                             logo, which needs the depth ordering this
;;                             removes. Written for Fighting Vipers 2's SEGA
;;                             screen and it was not the fix there either (that
;;                             was sprite_color + vtx_alpha), so it survives
;;                             purely as an investigation tool for the next
;;                             scene where translucent strips wrongly occlude
;;                             each other. An auto mode keyed on ISP_FEED_CFG
;;                             was tried and removed: nothing writes that
;;                             register, so it read 0 everywhere and behaved
;;                             exactly like on.)
;;   bg_poly         = on | off  (background polygon rendering. ISP_BACKGND_T's
;;                             3 vertices are normally only used for the EFB
;;                             clear color. on additionally barycentric-
;;                             extrapolates them into a full-screen textured/
;;                             Gouraud quad — needed by Who Wants to Be a
;;                             Millionaire's background — at the cost of an
;;                             extra texture bind + polygon draw every frame.
;;                             off (default): legacy, draws nothing extra.
;;                             Caused an FPS regression in other games when
;;                             left unconditionally on — enable per-game only.)
;;   layer_sort      = on | off  (layer-tiered translucent sort for 2D games
;;                             that submit the whole scene at ONE depth and
;;                             rely on PVR per-pixel autosort. Activates the
;;                             trans_sort machinery (implied, no need to set
;;                             trans_sort=on) and adds a tie tier at equal
;;                             depth: full-screen untextured REPLACE plates
;;                             draw first (background plates the game
;;                             expects autosort to bury), then VQ stage art
;;                             (ALL VQ — splitting VQ by address broke the
;;                             char select screen, keep VQ order legacy),
;;                             then 8bpp sprites on palette bank 32-47
;;                             (stage crowd), then everything else in
;;                             submission order — the backdrop can't erase
;;                             the fighters, while high-VRAM foreground VQ
;;                             (super auras, cut-ins, intro dust) and
;;                             fades/HUD keep painter compositing. Only the
;;                             full-screen plate tier is scene-gated (80+
;;                             VQ strips = battle); the VQ and crowd tiers
;;                             stay on unconditionally — gating those too
;;                             was tried and broke char select/
;;                             presentation (disabling crowd moved it into
;;                             the topmost tier instead of a neutral one).
;;                             Every tier is chosen from RENDER STATE only,
;;                             so this is game-agnostic — SF3 uses it too.
;;                             Was called hokuto_hack before 0.66; that name
;;                             now means the address hack below only.
;;                             off (default): submission order only —
;;                             Hokuto no Ken's backdrop covered its
;;                             fighters that way.)
;;   hokuto_hack     = on | off  (Hokuto no Ken ONLY. Refines the layer_sort
;;                             tier of VQ stage art using that game's own
;;                             hardcoded VRAM texture addresses: its stage 1
;;                             and 2 debris tiles are pushed behind the rest
;;                             of the tier, and one tile that must never be
;;                             visible is pushed behind everything. Those
;;                             tiles cannot be told apart from the fighters
;;                             by format + blend alone, which is why they are
;;                             keyed by address instead. Needs layer_sort=on
;;                             — on its own it does nothing — and the address
;;                             list is per-game AND per-scene, so leave it
;;                             off everywhere else. off (default).)
;;   puyo_hack       = on | off  (Puyo Puyo 4 ONLY. Folds that game's own two
;;                             hardcoded backdrop VRAM addresses into
;;                             layer_back_tex's tier-4 sort: the gameplay
;;                             playfields (puyos go dim behind the grid) and
;;                             the intro/main screen background (fading
;;                             UI/logo pieces get buried under it) — two
;;                             unrelated screens, same bug shape. Addresses
;;                             live in gxRend.cpp, not in this file; combines
;;                             with an explicit layer_back_tex list if one is
;;                             also set. Also a page-6 menu row, specifically
;;                             so this fix works without touching the cfg at
;;                             all (e.g. from inside Dolphin, where the SD
;;                             card isn't easy to edit). off (default).)
;;   isp_depth_func  = 0 | 1 | 2  (per-polygon isp.DepthMode -> GX depth
;;                             compare. DIFFERENT from layer_sort above: this
;;                             honors each polygon's actual DepthMode register
;;                             instead of the fixed GEQUAL painter compare.
;;                             0=off (legacy), 1=honor DepthMode on the
;;                             opaque/PT lists only (translucent stays GEQUAL,
;;                             matching real PVR autosort), 2=all lists.
;;                             Experimental — off by default.)
;;   isp_cull        = 0 | 1 | 2  (per-polygon isp.CullMode -> GX backface
;;                             culling. 0=off (legacy: never cull), 1=on,
;;                             2=on with the two cullable windings swapped
;;                             (use if 1 makes geometry vanish / look
;;                             inside-out). Experimental — off by default.)
;;   subpass_zclear  = on | off  (sub-pass depth-only Z clear. Re-parks the whole
;;                             Z buffer at a known W via a full-canvas GX_ALWAYS
;;                             quad, without touching color, so a geometry group
;;                             drawn right after (e.g. a hud_pass=2 PROTECT-mode
;;                             overlay) starts from a clean depth baseline
;;                             instead of whatever the main scene left behind.
;;                             off (default): single shared depth pass, legacy.
;;                             on: re-park before that later pass — opt-in
;;                             per-game.)
;;   poly_offset     = 0 | 1 | 2 | 3  (native polygon offset / Z bias tier,
;;                             applied to the Punch-Through list. Real hardware
;;                             has no glPolygonOffset register; this uses a
;;                             Z-texture in ADD mode (GX_SetZTexture) to add a
;;                             constant depth bias to every fragment, for
;;                             co-planar decals/shadows/road-markings that
;;                             PVR's tile order used to sort for free. 0
;;                             (default): off, legacy. 1..3: increasing bias
;;                             strength.)
;;   audio_buffers   = 0 | 1 | 2 | 3 | default  (forces
;;                             settings.emulator.AudioBuffers, the AICA
;;                             audio queue depth — see nullDC.cpp
;;                             LoadSettings() and wii/wii_audio.cpp. 0:
;;                             never blocks, samples are dropped on
;;                             overrun. 1..3: blocks the emulation thread
;;                             each push until fewer than N buffers are
;;                             queued — smoother audio pacing at the cost of
;;                             emulation speed on games that burst audio.
;;                             default (alias: auto, saved): explicitly
;;                             resets it to the saved/cfg value, undoing a
;;                             [default] section override or a previous
;;                             game's setting from earlier this boot.
;;                             Leaving the key out of a section entirely
;;                             does the same thing UNLESS an earlier section
;;                             already forced it — 'default' guarantees the
;;                             reset regardless of what ran before.)
;;   sh4_clock       = 150..200  (SH4 underclock — effective SH4 core clock in
;;                             MHz, clamped to [150,200]. 200 (default) is
;;                             real-Dreamcast full speed. Lower values budget
;;                             fewer emulated SH4 cycles per audio/video/RTC
;;                             frame, so the Wii renders more frames per second
;;                             (smoother), at the cost of the emulated machine
;;                             acting like a slower Dreamcast — CPU-heavy games
;;                             drop internal frames. Audio stays in sync. A
;;                             pure performance knob — test per game.)
;;   arm7_speed      = 0 | 1 | 2  (ARM7 sound-CPU speed divider stage. Effective
;;                             clock = ~10 MHz >> stage. The AICA driver free-runs
;;                             its poll/scan loop far more than any driver needs,
;;                             so 1 (half) or 2 (quarter) reclaims host CPU. Default
;;                             0 (off/legacy). Verify music/SFX timing by ear before
;;                             keeping — stage 2 has been found to break audio
;;                             timing.)
;;   fastmem         = on | off  (PPC-MMU fastmem for the SH4 dynarec. Maps the DC
;;                             address space via segment regs + a hashed page
;;                             table so JIT loads/stores are branchless; MMIO/SQ/
;;                             BIOS accesses DSI-fault once and get back-patched to
;;                             slow-path trampolines. Default on. Experimental —
;;                             A/B per game if it misbehaves.)
;;   bcache          = on | off  (flat dynamic-branch dispatch cache for the SH4
;;                             dynarec. Dynamic jumps (jmp/rts/bsrf) hit one 8-byte
;;                             {addr, code} entry instead of chasing cache[] ->
;;                             DynarecBlock across two cache lines + a counter
;;                             write. Default off (legacy). Perf preset — A/B.)
;;   fpu_pin         = on | off  (pins SH4 fr0-15 to real PPC FPU registers for the
;;                             whole session, the same scheme int GPRs already use.
;;                             Speeds up geometry-heavy games (fadd/fmul/fmac/
;;                             fipr/ftrv/cvt_* stop round-tripping through memory).
;;                             Default off — new and unproven, A/B per game,
;;                             especially anything 3D-transform heavy.)
;;   jit_align       = on | off  (pads each SH4-dynarec block entry to a 32-byte
;;                             Broadway L1 cache line, so every branch/link target
;;                             begins on a clean line boundary. Cache-hygiene only,
;;                             no logic change, marginal effect. Default off.)
;;   sched           = on | off  (unified cycle-deadline event scheduler. Fires
;;                             GD-ROM read-done, ch2/PVR/AICA-DMA completion,
;;                             render-done and TA list-end through one deadline
;;                             queue in true hardware order, instead of at their
;;                             own tier of the Medium/Slow/VerySlow timeslice
;;                             cascade. Default off. EXPERIMENTAL — A/B per game.)
;;
;; DEBUG LOGS (all default off, options page 6, output goes to /ndclog.txt on
;; the card — leaving one on costs card writes every frame, so turn it back off
;; once it has answered its question):
;;   debug_log_framebuffer2d = on | off  (answers "is framebuffer_2d worth
;;                             trying on this game?". Logs every bit-24 render
;;                             pass the framebuffer_2d preset would act on —
;;                             the ones that get dropped while it is off:
;;                               [PATH] 2D-blit : Try Enabling FRAMEBUFFER_2D PRESET
;;                               [PATH] 2D-after-3D : Try Enabling FRAMEBUFFER_2D PRESET
;;                             With framebuffer_2d=on the same lines confirm the
;;                             preset is doing the work instead. NO such line in
;;                             the log means the game never takes that path and
;;                             framebuffer_2d cannot help it. Self-quieting: the
;;                             first 8 of each kind, then one line per 600.)
;;   debug_message   = on | off  (renderer trace — the [PATH], [FB], [RTT]... lines)
;;   debug_loop      = on | off  (per-loop CPU/GD-ROM/IO trace. VERY slow.)
;;   debug_gdrom     = on | off  (GD-ROM / CDDA SPI command trace)
;;   debug_skip_tex  = 0 | <VRAM address>  (HIDES every polygon that uses one
;;                             texture. Diagnostic only: it REMOVES geometry, it
;;                             never fixes anything. Takes an address straight
;;                             off a [SCN] census line's addr= field, hex or
;;                             decimal, e.g. debug_skip_tex=0x52C000; 0 = off.
;;                             The options-page-6 row toggles it on/off in place,
;;                             so one build can A/B a scene as many times as you
;;                             like. Answers the question a census cannot: is an
;;                             element missing or washed out because something is
;;                             painted OVER it (hide the suspect and it appears
;;                             correctly), or because the element itself is drawn
;;                             wrong (hide everything around it and it is still
;;                             wrong)? This is how the Hokuto no Ken layering bug
;;                             was finally pinned down.)
;;
;; EXAMPLE FILENAMES THAT WOULD MATCH:
;;   [jojo]        matches  JoJos_Bizarre_Adventure.gdi  or  jojo_dc.cdi
;;   [daytonausa]  matches  DaytonaUSA.gdi  or  daytona_usa_2001.cdi
;;
;; [default] IS SPECIAL:
;;   Not matched against any filename — its fields are applied first, on
;;   every launch, before the per-game section below is matched. Per-game
;;   sections only need to list the fields that differ from [default].
;;   It is pre-filled here with the emulator's built-in (out-of-the-box)
;;   defaults, so editing it changes the baseline for every game at once.
;; --------------------------------------------------------------------------


[default]

;; General
ratio=fullscreen
speed_limiter=off
show_fps=off ; gameplay FPS/speed overlay
graphics=normal
gx=on
lod_bias=0.0
aniso=0x
tex_cache=normal
4bpp=ci4_fast
8bpp=ci8_fast
frameskip=0
framebuffer_2d=off
adv_alpha=on
blend_mode=on
fps_boost=off
punch_through=off
trans_sort=off
render_to_texture=off
split_screen=off

;; Graphics
fmv_format=rgb565
yuv_twiddle_fix=on ; this preset is likely to disapears
yuv_stride=texctl
vertex_color=on
decal_alpha=off
seam_fix=off ; it has to be a default off, can break some textures
fog=off
bg_poly=off
rgb565_opaque_alpha=off
jojo_fix=off
offset_color=on

;; Depth & Width
legacy_depth    = on | off
depth_clip=1
fixed_depth=0
hud_pass=0
subpass_zclear=off
ppz_write=on
isp_depth_func=0
isp_cull=0

autosort=0
layer_sort=off
list_order=off ; opaque list drawn first when a game submits it after the TR list
layer_back_tex=0 ; address of a backdrop texture to sort behind the sprites (0 = off)
x_scaler=on
y_scaler=off
h_scaler=off
canvas_width=0

poly_offset=0

;; Audio
audio_buffers=default
cdda=on
mute_pcm16=off

;; Core
accuracy=fast ; testing FAST as default in alpha0.52
async_render=on
render_delay=off
tmem_cache=on
sh4_clock=200 ; SH4 core clock in MHz (150..200); 200=full speed, lower=faster host/slower game
arm7_speed=0
jit_sbp=1     ; JIT Stale Block Protection stage (0=off, 1=known self-modifying addrs, 2=every RAM block)
fastmem=on    ; PPC-MMU fastmem for the SH4 dynarec
bcache=on    ; flat dynamic-branch dispatch cache for the SH4 dynarec
dyn_ic=2     ; inline cache on dynamic branch exits, incl. RTS; Wii-measured +0.98%
fpu_pin=on   ; pin SH4 fr0-15 to real PPC FPU registers (experimental)
jit_align=on ; 32-byte block-entry alignment (cache hygiene, marginal)

;; Experimental Stuff & Debug
mipmap=off
dma_fix=on    ; ch2/PVR/Sort/AICA-G2 DMA correctness fixes (NullDC PSP port parity)
trans_zwrite=on ; off is the experimental stuff
sched=off     ; unified cycle-deadline event scheduler (experimental)
dino_crisis_inventory_hack=off
hokuto_hack=off
interpreter=off ; not wired from this file

;; Debug logs (all off — they write to /ndclog.txt on the card every frame)
debug_log_framebuffer2d=off ; log the passes framebuffer_2d would act on ("Try Enabling...")
debug_message=off ; renderer trace ([PATH], [FB], [RTT]...)
debug_loop=off    ; per-loop CPU/GD-ROM/IO trace (VERY slow)
debug_gdrom=off   ; GD-ROM / CDDA SPI command trace
debug_skip_tex=0  ; hide one texture's polygons to see what covers them (0 = off)

;; Controls
players=4
controller=standard

[102 dalmatians][dalmatians][dalmatiens][dalmatas][Dalmatiner]
tex_cache=very_fast
fixed_depth=2 ; needed 
hud_pass=2 ; untested but probably needed

<4 wheel thunder><4wheelthunder>
tex_cache=very_fast+ ; fast works also
vq_cmbr=on ; needed for very_fast+
fps_boost=off
split_screen=on ; needed for 2 player splitscreen
h_scaler=on ; needed for 2 player splitscreen


[4 wheel thunder][4wheelthunder]
tex_cache=very_fast+ ; fast works also
vq_cmbr=on ; needed for very_fast+
fps_boost=on ; seems to work flawlessly
split_screen=on ; needed for 2 player splitscreen (may reduce FPS in single player)
h_scaler=on ; needed for 2 player splitscreen

[24 hours of le Mans][le mans][24]
tex_cache=very_fast+ ; ok
vq_cmbr=on ; needed for very_fast+
render_delay=on ; needed for faster loading ?
split_screen=multipass ; 2P splitscreen flicker

[aerowings 2][aerowings2]
tex_cache=fast
; jojo_fix=on ; doesn't fix the FPS drop
; render_delay=on ; doesn't fix the FPS drop

[aerowings][aerowings]

[Alone in The Dark][alone]
ppz_write=off ; fix remanence on character
; isp_depth_func=2 ; fix remanence also ? Will tell you later
trans_sort=on ; fix logo in intro
jojo_fix=on ; more FPS on main gameplay (it's 8BPP)


[aqua GT] ; Aqua GT
tex_cache=fast ; very fast works but buggy. Quality display bronze cup in menu
punch_through=on ; Has effect on 3/2/1 start fire
blend_mode=off ; make the sky appears, but wrong 3/2/1 start fire
split_screen=both ; on make the boat selection a bit better. but also bug in intro (cache like bug I'd say)
; Sun Glare buggy, not sure of the texture format and how to fix

[army men][armymen]
tex_cache=very_fast ; other are too slow
fixed_depth=2 ; needed
hud_pass=2 ; probably needed

[Adventure of buzz][buzz]
tex_cache=very_fast

[wiiu] ; guard line — stop old version reading <> as plain [] match
<bangai> ; Wii U
graphics=normal ; 480i mode
canvas_width=320 ; absolutely needed

[bangai]
graphics=low ; 240p mode ("Progressive")
canvas_width=320 ; absolutely needed


[berserk]
offset_color=off ; mandatory or white textures


[wiiu] ; guard line — stop old version reading <> as plain [] match
<Black Matrix><blackmatrix> ; Wii U
graphics=normal ; 480i ?

[Black Matrix][blackmatrix]
graphics=low ; 240p mode

[bomberman] ; Bomberman Online
tex_cache=normal ; safer (fast is trash by the way)
fmv_format=rgb565
trans_sort=on ; needed for bombs or else they hide what's behind when exploding

[bomberman] ; Bomberman Online
tex_cache=very_fast+ ; Works with vq_cmbr // Same performance with tex_cache=normal
vq_cmpr=on ; needed when using very_fast/+
trans_sort=on ; needed for bombs or else they hide what's behind when exploding
fmv_format=rgb565
fastmem=on ; confirmed working in a062


[blue stinger][bluestinger]
tex_cache=fast ; very_fast has some minor bugs
offset_color=off ; needed for blue stinger logo and water

[buggy heat]
tex_cache=very_fast+ ; CONFIRMED on Wii: very_fast speed, correct menus, no texture
;   glitches. Needs 4bpp/8bpp left on the [default] ci4_fast / ci8_fast, which decode to
;   4 and 8 bits per texel and so fit the address-derived slot; the OPTIMIZED and rgb565
;   variants bake the palette into 16bpp and would push every palette texture into the
;   overflow arena instead.
vq_cmpr=on ; CONFIRMED: VQ was the last format too big for its slot here (~314 KB decoded
;   each, against a 4 MB arena), which is what left the texture glitches. CMPR shrinks it
;   4x so it fits the slot outright.
; tex_cache=very_fast ; the old setting: same speed, but glitched textures
legacy_depth=on ; help display both VMU intro message and gameplay
; fixed_depth=2 ; good but doesn't display VMU screen
; split_screen=on ; needed for 2 players (can lower FPS even in single mode)
split_screen=on ; needed for 2 players (can lower FPS even in single mode)

[capcomvssnk2][capcom vs snk 2][capcom vs. snk 2]
render_delay=on ; mandatory or 5 Fps max
tex_cache=quality ; characters in gameplay crappy in normal
seam_fix=on ; seam on characters during gameplay


[wiiu] ; guard line — stop old version reading <> as plain [] match
<Capcom vs. SNK Pro><SNK Pro> ; Wii U
graphics=normal ; 480i
render_delay=on ; mandatory or 5 Fps max
seam_fix=on ; 
trans_sort=on ; a bit better (to the cost of fps)

[Capcom vs. SNK Pro][capcomvssnk][capcom vs snk][capcom vs. snk]
; graphics=low ; 240p with L + Start. Can remove seam_fix in that case
render_delay=on ; mandatory or 5 Fps max
seam_fix=on ; seam line on characters during gameplay
trans_sort=on ; a bit better (to the cost of fps)


[carrier]
offset_color=off ; mandatory or white textures
tex_cache=fast ; Very_fast is buggy

[wiiu] ; guard line — stop old version reading <> as plain [] match
<castlevania>
accuracy=accurate
graphics=normal
tex_cache=fast ; better

[castlevania]
accuracy=accurate
graphics=normal
tex_cache=very_fast ; fast give better visual but I use this game as benchmark
arm7_speed=0 ; it's for my benchmark
audio_buffers=1 ; it's for my benchmark

[wiiu] ; guard line — stop old version reading <> as plain [] match
<cannonspike><cannon spike> ; Wii U
fixed_depth=2 ; Tight — fixes the Z-fighting
hud_pass=2    ; keep the HUD on top (Z-write); use hud_pass=1 if scene disappears
tex_cache=normal ; safer
fastmem=on

[cannonspike][cannon spike] ; Wii
fixed_depth=2 ; Tight — fixes the Z-fighting
hud_pass=2    ; keep the HUD on top (Z-write); use hud_pass=1 if scene disappears
tex_cache=fast ; works in fast so yeah
fastmem=on ; working


[Charge 'n blast][Charge and blast][blast]
tex_cache=fast ; faster than normal. Very_fast+ = doesn't seem faster
fps_boost=on ; does work so yeah

[wiiu] ; guard line — stop old version reading <> as plain [] match
<chuchu> ; Chu Chu Rocket ! ; Wii U
depth_clip=1 ; needed or intro/menu invisible on real Wii (Dolphin doesn't Z-clip)
adv_alpha=on ; necessary for some elements
accuracy=fast
async_render=on ; Faster but 1 frame input-lag. Wii U can avoid this probably
tmem_cache=on ; Faster ?
framebuffer_2d=on ; on or off, no difference
frameskip=auto ; can help in slowdown (mouse mania)
graphics=normal ; low could make the game quicker (1 fps more at max)
tex_cache=very_fast+ ; very fast works, very_fast+ should fix a tiny display bug that happens some times
4bpp=ci4_fast ; chuchu doesn't seem to use 4bpp anyway
8bpp=ci8_fast ; chuchu doesn't seem to use 8bpp anyway
jojo_fix=on ; not necessary but could make the game quicker, remove if some glitches appears
seam_fix=on ; needed to avoid thin lines in main gameplay
cdda=on ; chuchu uses CD-Audio music
mute_pcm16=on ; couldn't resolve that strange delay or slowness of PCM16 in chuchu. Muted till I got fix
fastmem=on

[chuchu] ; Chu Chu Rocket !
depth_clip=1 ; needed or intro/menu invisible on real Wii (Dolphin doesn't Z-clip)
adv_alpha=on ; necessary for some elements
accuracy=fast
async_render=on ; Faster but 1 frame input-lag. Wii U can avoid this probably
tmem_cache=on ; Faster ?
framebuffer_2d=on ; on or off, no difference
frameskip=auto ; can help in slowdown (mouse mania)
graphics=normal ; low could make the game quicker (1 fps more at max)
tex_cache=very_fast+ ; very fast works, very_fast+ should fix a tiny display bug that happens some times
4bpp=ci4_fast ; chuchu doesn't seem to use 4bpp anyway
8bpp=ci8_fast ; chuchu doesn't seem to use 8bpp anyway
jojo_fix=on ; not necessary but could make the game quicker, remove if some glitches appears
seam_fix=off ; off, using LOW
cdda=on ; chuchu uses CD-Audio music
mute_pcm16=on ; couldn't resolve that strange delay or slowness of PCM16 in chuchu. Muted till I got fix
fastmem=on

[chicken run][chickenrun]
tex_cache=fast ; very fast not tested
blend_mode=on ; needed for some minor stuff

[coaster works][coasterworks]
tex_cache=very_fast
trans_sort=on ; needed for some stuff to appears
rgb565_opaque_alpha=on ; needed ?
decal_alpha=on ; needed ?

[confidentialmission][confidential mission]
tex_cache=fast ; okish. very fast not tested

[crazy taxi 2][crazytaxi2] ; more specific first
depth_clip=1 ; needed or intro/menu invisible on real Wii (Dolphin doesn't Z-clip)
tex_cache=very_fast+
vq_cmpr=on
graphics=normal
decal_alpha=on ; Slower on Dolphin but on real Wii doesn't seem to have any effect (TEV algorythm)
vertex_color=on

[crazy taxi][crazytaxi][crazytaxi1]
depth_clip=1 ; needed or intro/menu invisible on real Wii (Dolphin doesn't Z-clip)
tex_cache=very_fast+
vq_cmpr=on
graphics=normal
decal_alpha=on ; Slower on Dolphin but on real Wii doesn't seem to have any effect (TEV algorythm)
vertex_color=on
seam_fix=on ; fix main logo

[cyber troopers][virtual on][virtual-on]
graphics=very_fast+ ; works with vq_cmpr
vq_cmpr=on ; mandatory with very_fast/+
legacy_depth=off ; doesn't seems to fix anything
seam_fix=on ; fix 2D menu. May cause texture glitch ingame, beware

[dance dance revolution club mix][club mix][club version]
tex_cache=very_fast+ ; works. normal tested also works
vq_cmbr=on ; needed for very_fast/+
audio_buffers=1 ; game is fast enough for it. And it's a rythm game
h_scaler=on ; mandatory
layout_ddr_select=on ; GameCube Z button = analog stick down (Select: dancer/
                     ; skin/sequence type, Left/Right/Shuffle option codes)

[dance dance revolution 2nd mix][2nd mix]
tex_cache=very_fast+ ; works. normal tested also works
vq_cmbr=on ; needed for very_fast/+
audio_buffers=1 ; game is fast enough for it. And it's a rythm game
h_scaler=on ; mandatory
layout_ddr_select=on ; GameCube Z button = analog stick down (Select: dancer/
                     ; skin/sequence type, Left/Right/Shuffle option codes)

[wiiu] ; guard line — stop old version reading <> as plain [] match
<dave mirra> ; Wii U
tex_cache=fast ; safer
fps_boost=off ; safer

[dave mirra]
tex_cache=very_fast ; very_fast works so yeah
fps_boost=on ; seems to work

[daytona]
graphics=very_fast+ ; works with vq_cmpr
vq_cmpr=on
fps_boost=on ; seems ok to use it
split_screen=off ; -20% FPS, probably better off for Wii and on for Wii U in that case
trans_sort=on ; fix menu (wii & dolphin) and some flickering (dolphin)

[dead or alive 2]
graphics=very_fast+ ; works with vq_cmpr
vq_cmpr=on

<deadly skies>
tex_cache=fast ; safer
; fixed_depth=2 ; Tight — fixes some Z-fighting but make menu disapears (tested on dolphin)
h_scaler=on ; mandatory : fix the half screen display
trans_sort=off ; Heavy FPS heavily drops with that (3x)

[deadly skies]
tex_cache=very_fast+ ; Works.
vq_cmbr=on ; needed with very_fast+
; fixed_depth=2 ; Tight — fixes some Z-fighting but make menu disapears (tested on dolphin)
h_scaler=on ; mandatory : fix the half screen display
trans_sort=off ; Heavy FPS heavily drops with that (3x)



[deathcrimson][death crimson]
controller=lightgun
accuracy=fast ; untested

[deep fighter][deepfighter] ; Criterion Unreleased game
tex_cache=very_fast+ ; fast and normal doesn't display wait screen. Water bug thus
vq_cmbr=on

[demolitionracer][demolition racer]
graphics=normal
ppz_write=on ; needed
split_screen=multipass ; 2P splitscreen flicker (one render pass per viewport)


[dinocrisis][dino crisis] ; Dino Crisis
tex_cache=very_fast+ ; very_fast works also
vq_cmbr=on ; needed for very_fast/+
trans_sort=on ; needed ? May consume a bit of FPS
; punch_through=on ; needed ? May consume a bit of FPS
sprite_color=on ; not sure if needed
vtx_alpha=on ; needed for the first riddle (= enigma) to fix transparency on the window
h_scaler=on ; needed for intro FMV and probably all FMV in the game
dino_crisis_inventory_hack=on ; inventory item preview icon shows up in every cache mode, not only Quality

[wiiu] ; guard line — stop old version reading <> as plain [] match
<dinosaurs>
tex_cache=fast ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on)
fps_boost=off ; safer

[dinosaurs]
tex_cache=fast ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on)
fps_boost=on ; works

[donald][duck]
tex_cache=fast ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on)

[dragon riders][dragonriders]
tex_cache=normal ; very_fast not tested I think. Fast ok. Normal to avoid crash at the intro ?

[dragon's blood][dragons blood]
tex_cache=fast ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on)
fixed_depth=2 ; Tight needed to display menu
hud_pass=2

[ECW The Energy Rule][ECW]
tex_cache=fast ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on)
punch_through=on ; seems needed for some menu

[Ecco] ; Ecco The Dolphin
tex_cache=very_fast+ ; fast also works good
vq_cmbr=on
fog=off ; doesn't seem to change anything

[evildead][evil dead]
tex_cache=very_fast+
vq_cmbr=on


[wiiu] ; guard line — stop old version reading <> as plain [] match
<evolution 2><evolution2>
tex_cache=normal ; safer

[evolution 2][evolution2]
tex_cache=fast ; try very_fast also

[wiiu] ; guard line — stop old version reading <> as plain [] match
<evolution1><The World of Sacred Device>
tex_cache=normal ; safer

[evolution1][The World of Sacred Device]
tex_cache=fast ; try very_fast also


[wiiu] ; guard line — stop old version reading <> as plain [] match
<exhibition> ; Exhibition of speed
tex_cache=normal ; fast or normal are safer

[exhibition] ; Exhibition of speed
tex_cache=very_fast+
vq_cmbr=on

[Fast Striker][faststriker]
graphics=low ; 240p game
punch_through=on ; 

[fasterthanspeed][faster than speed]

[wiiu] ; guard line — stop old version reading <> as plain [] match
<fighting force><fightingforce> ; fighting force 2 (Wii U)
tex_cache=normal ; fast or normal
punch_through=on ; needed for lights

[fighting force][fightingforce] ; fighting force 2
tex_cache=very_fast+ ; or fast with vq_cmbr=off . Same performance.
vq_cmbr=on
punch_through=on ; needed for lights


[fighting Vipers][fightingvipers][fighting_vipers][fighting-vipers]
tex_cache=very_fast+ ; very_fast seems ok but very_fast+ and vq_cmbr is safer
vq_cmbr=on
vertex_color=on ; needed for intro to be black instead of white
vtx_alpha=on ; needed for intro to have transparency

[fur fighters][furfighters]
tex_cache=normal ; only tested this for now
fixed_depth=2 ; Z fighting
hud_pass=2
trans_sort=on ; this or punch trough for menu
punch_through=on ; this or trans_sort for menu
render_delay=on ; needed ?
jojo_fix=on  ; needed 
offset_color=off ; needed ?

[wiiu] ; guard line — stop old version reading <> as plain [] match
<gauntlet> ; (Wii U)
tex_cache=fast ; safer (normal works also)

[gauntlet]
tex_cache=very_fast+ ; works with vq_cmpr=on
vq_cmpr=on ; for very_fast+

[wiiu] ; guard line — stop old version reading <> as plain [] match
<gigawind>
tex_cache=normal ; safer

[gigawind]
tex_cache=fast ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on)

[grandia] ; grandia 2
tex_cache=fast ; very_fast works with minor flaws
fixed_depth=0 ; Worse if put on 2, keep 0
trans_sort=on ; needed for some transparency it seems (smoke)
; trans_sort=on ; needed ?

[wiiu] ; guard line — stop old version reading <> as plain [] match
<grinch>
tex_cache=fast ; safer, no FPS loss. Same for normal
fixed_depth=2 ; Fix Z-Fighting

[grinch]
tex_cache=very_fast ; 
fixed_depth=2 ; Fix Z-Fighting

[gta2][grand theft auto] ; Gran Theft Auto 2
adv_alpha=on
tex_cache=normal ; normal with tmem_cache on OR fast with tmem_cache = off 
; tmem_cache=off
; very_fast+ and vq_cmbr doesn't give better result (and even worse)

[wiiu] ; guard line — stop old version reading <> as plain [] match
<guilty gear><guiltygear> ; Guilty Gear X
tex_cache=quality ; with Wii U speed it's okish I guess

[guilty gear][guiltygear] ; Guilty Gear X
tex_cache=normal ; acceptable, needs Quality to reduce cache problems. Maybe a regression

[gunbird][gunbird 2] ; Gunbird 2
graphics=low ; 240p game


[wiiu] ; guard line — stop old version reading <> as plain [] match
<gunlord>
graphics=normal ; 480i game ?

[gunlord]
graphics=low ; 240p game


[Godzilla Generations Maximum Impact]
tex_cache=normal ; very-fast = no FPS improvement. Cache normal is globally safe

[headhunter]
tex_cache=fast
; trans_sort=on ; doesn't fix bad alpha
; punch_trough=on ; doesn't fix bad alpha
; render_to_texture ; doesn't fix bad alpha

[hokuto][fistofthenorthstar][fist of the north star]
tex_cache=very_fast+
vq_cmbr=on
layer_sort=on ; one-depth 2D scene: tiered TR sort (stage art -> crowd -> painter)

hokuto_hack=on ; + specific ram address hack for stage 1 & 2 debris tiles
list_order=on ; puyo 4 additional fixes
seam_fix=on ; health bar needs that in NORMAL/HIGH/EXTRA 

[houseofthedead][house of the dead]
controller=lightgun
accuracy=fast ; untested


[hydrothunder][hydro thunder]
tex_cache=very_fast+ ; or fast (bit slower it seams)
vq_cmbr=on
seam_fix=off ; cause tiny bug on water if on
arm7_speed=1
audio_buffers=1

[iron aces][ironaces]
tex_cache=very_fast ; works !
trans_sort=on       ; fix menu
offset_color=off    ; fix white planes
fixed_depth=2       ; avoid Z-Fighting ?
punch_through=on    ; Fix transparency problem
; render_to_texture=on   ; needed ?

[illbleed][ill bleed]
tex_cache=fast ; very_fast playable but meh
trans_sort=on  ; fix menu


[Jeremy McGrath Supercross][jeremy][mcgrath][supercross]
tex_cache=fast ; very_fast is meh



[jetsetradio][jet set radio][jetgrindradio][jet grind radio] ; Jet Grind Radio in USA
graphics=normal ; mandatory or z-fighting/noise with LOW
tex_cache=very_fast+ ; needs vq_cmbr=on
vq_cmbr=on ; needed by very_fast/+
vertex_color=on
trans_sort=off ; ON make smoother level transition, but buggy GUM's face in intro. ON has less fps also

[jissen][pachislo] ; Japanese Pachi slot
offset_color=off

[wiiu] ; guard line — stop old version reading <> as plain [] match
<jojo> ; Wii U
graphics=low ; 240p game
4bpp=ci4_fast
jojo_fix=on
trans_sort=on ; for fixing 50/60hz menu

[jojo]
graphics=low ; 240p game
4bpp=ci4_fast
jojo_fix=on

; KOF games


[wiiu] ; guard line — stop old version reading <> as plain [] match
<kingoffightersdreammatch><king of fighters Dream Match><Dream Match> ; Wii U
graphics=low ; 240p game
tex_cache=normal ; safer
fixed_depth=2 ; needed
canvas_width=320 ; mandatory

; Zeox testing (Wii U)
; graphics=low ; 240p game
; canvas_width=320
; framebuffer_2d=on
; async_render=on
; trans_sort=on
; accuracy=balanced
; fpu_pin=on
; jit_sbp=on
; jit_align=on

[kingoffightersdreammatch][king of fighters Dream Match][Dream Match]
graphics=low ; 240p game
tex_cache=fast ; fast is a bit buggy but ok
fixed_depth=2 ; needed
canvas_width=320 ; mandatory



[wiiu] ; guard line — stop old version reading <> as plain [] match
<kingoffightersevolution><king of fighters evolution> ; Wii U
graphics=normal ; use 480p mode
tex_cache=normal ; fast = trash
fixed_depth=2 ; needed
; hud_pass=2 ; seems not needed
canvas_width=320 ; mandatory

[kingoffightersevolution][king of fighters evolution] ; Wii
graphics=low ; use 240p mode
tex_cache=normal ; fast = trash
fixed_depth=2 ; needed
; hud_pass=2 ; seems not needed
canvas_width=320 ; mandatory

[kingoffighters99][king of fighters 99]
graphics=low ; 240p game


[lastblade2][last blade][last blade 2] ; The Last Blade 2
graphics=low ; 240p game
canvas_width=320 ; probably needed


[wiiu] ; guard line — stop old version reading <> as plain [] match
<Last Hope><lasthope> ; Wii U
graphics=normal ; 480i ?

[Last Hope][lasthope]
graphics=low ; 240p game

[legacy of kain][legacyofkain]
tex_cache=fast ; works nice
; fixed_depth=2 ; needed for menu
; hud_pass=2 ; needed ?

[wiiu] ; guard line — stop old version reading <> as plain [] match
<looney tunes space><looneytunes> ; Wii U
tex_cache=normal ; safer
fixed_depth=2 ; needed
render_delay=on ; fix some slow fps and flickering ?

[looney tunes space][looneytunes]
tex_cache=fast ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on)
fixed_depth=2 ; needed
render_delay=on ; fix some slow fps and flickering ?

[wiiu] ; guard line — stop old version reading <> as plain [] match
<maken X><maken> ; Wii U
tex_cache=normal ; safer

[maken X][maken]
tex_cache=fast ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on)


[magforce] ; Magforce Racing (also Known as "Killer Loop")
adv_alpha=off ; not necessary
tex_cache=fast ; veryfast = crap. Normal : for Wii U
punch_through=on ; absolutely necessary during gameplay
; ppz_write=on ; Needed to avoid remanence ?

[mars matrix][marsmatrix]
tex_cache=very_fast+ ; need vq_cmbr=on
vq_cmbr=on ; need vq_cmbr=on
hud_pass=2 ; 0 = doesn't work

[mr driller][driller] ; Mr Driller 
tex_cache=very_fast+ ; mostly good now with vq_cmbr=on / fast/normal is slow
vq_cmbr=on
trans_sort=on ; mandatory for most menu
tmem_cache=off ; safer with off I think
offset_color=off ; might fix the background in main gameplay
4bpp=ci4_fast ; faster
8bpp=ci8_fast ; faster
vertex_color=on ; needed for background to be textured instead of white
vtx_alpha=on ; needed transparency in menu

[marsmatrix][mars matrix] ; Mars Matrix (CPS2)
tex_cache=very_fast+ ;
vq_cmbr=on

[wiiu] ; guard line — stop old version reading <> as plain [] match
<marvelvscapcom2><marvel vs capcom 2><marvel vs. capcom 2><new age of heroes> ; Wii U
; graphics=low ; On Wii it could benefit (tiny fps boost = 0.5 fps)
graphics=normal
tex_cache=normal
render_delay=on ; paces its main loop off list-complete/render-done IRQs; instant IRQs = "130% speed" but ~7 FPS
seam_fix=on ; needed ?
fastmem=on

[marvelvscapcom2][marvel vs capcom 2][marvel vs. capcom 2][new age of heroes]
; graphics=low ; On Wii it could benefit (tiny fps boost = 0.5 fps)
graphics=normal
tex_cache=fast
render_delay=on ; paces its main loop off list-complete/render-done IRQs; instant IRQs = "130% speed" but ~7 FPS
fastmem=on

[wiiu] ; guard line — stop old version reading <> as plain [] match
<marvelvscapcom><marvel vs capcom><marvel vs. capcom><clash of super heroes>
graphics=low ; 240p game
tex_cache=normal ; safer
; FPS/speed doesn't show on dolphin (?)

[marvelvscapcom][marvel vs capcom][marvel vs. capcom][clash of super heroes]
graphics=low ; 240p game
tex_cache=fast ; very_fast is slower in menu (!?)
; FPS/speed doesn't show on dolphin (?)

[wiiu] ; guard line — stop old version reading <> as plain [] match
<metropolis street racer><msr> ; Wii U
tex_cache=normal ; safer
; may need more options

[metropolis street racer][msr]
tex_cache=fast ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on)
; may need more options

[millionaire] ; Who Wants to Be a Millionaire
bg_poly=on ; needed for the background
audio_buffers=1 ; speed can handle it even on Wii probably

[monaco] ; Monaco grand Prix
tex_cache=very_fast+ ; didn't test other
vq_cmbr=on ; needed with very_fast/+
trans_sort=on ; fix black alpha but lower fps

[mortal kombat gold][mortal kombat]
tex_cache=very_fast ; very_fast works

[wiiu] ; guard line — stop old version reading <> as plain [] match
<ms pacman><pacman><pac man> ; Wii U
tex_cache=normal ; safer

[ms pacman][pacman][pac man]
tex_cache=fast ; very_fast not tested

[nba 2k]
tex_cache=fast ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on)

[nba hoops][hoops]
tex_cache=fast ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on) in menu
fps_boost=on ; probably can help
fixed_depth=2 ; help Z fighting
; hud_pass=2 needed ?

[nba showtime][nbashowtime][nba show time]
tex_cache=fast ; very_fast is not very good
fmv_format=cmpr ; defaut and cmpr doesn't show. Maybe in cache quality they show
fps_boost=off ; unplayable with on
fixed_depth=2 ; help Z fighting
; hud_pass needed ?

[nfl blitz 2000][nfl blitz]
tex_cache=fast ; very_fast not tested

[nfl quaterback][nfl blitz]

[nhl 2k][nhl]


[Neo XYX][neoxyx]
graphics=low ; 240p game ?


[omicron][nomad] ; Omicron The Nomad Soul (matches "Omicron", "Nomad Soul", "NomadSoul", ...)
x_scaler=on ; renders 1280 wide with the PVR X scaler; without this only the left half shows
tex_cache=very_fast+ ; fast not very good. normal good
vq_cmbr=on
fixed_depth=2 ; needed
hud_pass=2 ; probably needed

[wiiu]
<outtriger>
tex_cache=normal ; safer
split_screen=on

[outtriger]
tex_cache=very_fast+ ; fast not tested / normal good
vq_cmbr=on
split_screen=on


[wiiu] ; guard line — stop old version reading <> as plain [] match
<panzer front><panzer>
tex_cache=fast ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on)
trans_sort=on ; helps for menu transparency

[panzer front][panzer]
tex_cache=fast   ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on)
trans_sort=on    ; helps for menu transparency
punch_through=on ; for tank's smoke 
fps_boost=on     ; workish, can help with tank smoke ?

[wiiu] ; guard line — stop old version reading <> as plain [] match
<Plasma Sword><Nightmare of Bilstein> ; Wii U
graphics=normal ; 480i ?

[Plasma Sword][Nightmare of Bilstein]
graphics=low ; 240p


[pod] ; POD 2
tex_cache=very_fast+ ; works !
vq_cmbr=on ; needed for very_fast/+
trans_sort=on ; needed for transparency
vtx_alpha=off ; needed for shield around the car
rgb565_opaque_alpha=off ; needed or menu doesn't appear. Maybe this preset was removed, I don't remember
split_screen=on ; needed for 2 player. May lower FPS even in 1P

[wiiu] ; guard line — stop old version reading <> as plain [] match
<penpen><pen pen>
tex_cache=normal ; safer

[penpen][pen pen]
tex_cache=fast ; very_fast is not very good


[phantasy star online v2][phantasy star online ver2]

[planet ring][planetring]
tex_cache=fast ; seems to work... very_fast not tested

[plasma world][plasmaworld] ; star gladiator episode 2
tex_cache=normal ; very_fast isn't very good. Fast is meh. Normal doesn't seem to suffer of much fps loss compared to fast

[wiiu] ; guard line — stop old version reading <> as plain [] match
<powerstone2><power stone 2> ; Wii U
tex_cache=normal ; safer
8bpp=ci8_fast ; absolutely needed or black character
fixed_depth=2 ; Tight — fixes the Z-fighting

[powerstone2][power stone 2]
tex_cache=fast ; no notable changes
; 4bpp=4bpp_optimized
8bpp=ci8_fast ; absolutely needed or black character
fixed_depth=2 ; Tight — fixes the Z-fighting
fps_boost=on ; untested for now

[wiiu] ; guard line — stop old version reading <> as plain [] match
<powerstone><power stone> ; Wii U
tex_cache=very_fast+ ; works faster than "fast"
vq_cmbr=on ; needed with very_fast/+

[powerstone][power stone] ; Wii
tex_cache=very_fast+ ; works faster than "fast"
vq_cmbr=on ; needed with very_fast/+
fps_boost=on ; can be used on Wii. minor glitches on loading screen


[wiiu] ; guard line — stop old version reading <> as plain [] match
<Princess Maker Collection><PrincessMakerCollection> ; Wii U
graphics=normal ; 480i ?

[Princess Maker Collection][PrincessMakerCollection]
graphics=low ; 240p

[prince of persia][arabian nights][pop]
tex_cache=very_fast+ ; works faster than "fast"
vq_cmbr=on ; needed with very_fast/+
speed_limiter=off
audio_buffers=1

[Pro Pinball] ; untested
tex_cache=very_fast+ ; works faster than "fast"
vq_cmbr=on ; needed with very_fast/+

[Propeller arena][propellerarena][propeller]
tex_cache=fast
fixed_depth=2
hud_pass=2

[wiiu] ; guard line — stop old version reading <> as plain [] match
<projectjustice><project justice><rival schools>
tex_cache=normal ; works. very_fast need to be re-tested (very_fast+ and vq_cmbr=on). Fast is also meh.
blend_mode=on ; needed or black for alpha
trans_sort=on ; fix Wild Daigo's aura over health bar
tmem_cache=on ; zeox-gamer suggestion
arm7_speed=1
offset_color=off ; remove some withish stuff ?
; framebuffer_2d=on ; zeox-gamer suggestion

[projectjustice][project justice][rival schools] ; Rival Schools
tex_cache=normal ; works. very_fast need to be re-tested (very_fast+ and vq_cmbr=on). Fast is also meh.
blend_mode=on ; needed or black for alpha
trans_sort=on ; fix Wild Daigo's aura over health bar (and other effect)
arm7_speed=1
tmem_cache=on ; zeox-gamer suggestion
offset_color=off ; remove some withish stuff ?
; depth_clip=0 
; fixed_depth=2
; hud_pass=2
; framebuffer_2d=on ; zeox-gamer suggestion

[wiiu] ; guard line — stop old version reading <> as plain [] match
<Puyo Puyo DA><PuyoPuyoDA> ; Wii U
tex_cache=very_fast+ ; very_fast works too and help boost fps !
trans_sort=on ; helps for transparency

[Puyo Puyo DA][PuyoPuyoDA]
tex_cache=very_fast+ ; very_fast works too and help boost fps !
trans_sort=off ; helps for transparency, disabled on the normal Wii
fps_boost=off ; can help but really make the game not displaying very good


[Puyo Puyo 4][PuyoPuyo4]
tex_cache=very_fast+ ; very_fast works too and help boost fps !
vq_cmbr=on
trans_sort=on ; needed ?
vtx_alpha=on ; needed ? I think it fix black transparency of the "4" in into
list_order=on ; gameplay was hidden by the two full-screen opaque background
              ; plates (058000 512x512 + 0D8000 256x512): they are in the OPAQUE
              ; list but submitted LAST, so submission order drew them on top
puyo_hack=on ; gameplay grid (0x118000) + intro backdrop (0x054000), both
             ; submitted AFTER the content standing in them, hidden behind it.
             ; Addresses are hardcoded in gxRend.cpp (PUYO_HACK()), not here --
             ; also toggleable from the PUYO HACK row on page 6, no cfg edit
             ; needed. Equivalent to layer_back_tex=0x118000,0x054000.



[quake 3][quake3][quake III] ; has a Wii Native port
tex_cache=very_fast+
vq_cmbr=on
fixed_depth=2 ; slow down menu but fix black flickering ingame
ppz_write=off ; needed to fix black Remanance
split_screen=multipass ; not working for now. Experimental branch : quake3-splitscreen

[rayman] ; Rayman 2
tex_cache=very_fast ; very_fast works good and great fps. Minor bugs. Fast is very slow unfortunatly
fixed_depth=2       ; Tight — fixes the Z-fighting
hud_pass=2          ; keep the HUD on top (Z-write); use hud_pass=1 if scene disappears
subpass_zclear=on   ; experimental, needs testing
trans_sort=on       ; for lumens


[ready2rumble2][ready 2 rumble boxing round 2]
tex_cache=very_fast ; works !
x_scaler=off   ; mandatory, else game display only half the screen
; fps_boost=on ; can work but trash in player selection
arm7_speed=1   ; 5 Mhz works

[ready2rumble][ready 2 rumble]
tex_cache=very_fast ; fast if something is janky
x_scaler=off   ; mandatory, else game display only half the screen

[Record of lodoss war][lodoss war]
tex_cache=fast ; untested
fixed_depth=2  ; untested (for Z-fighting)
hud_pass=2     ; keep the HUD on top (Z-write); use hud_pass=1 if scene disappears

[red dog]
tex_cache=fast ; very_fast is meh
fixed_depth=2  ; untested (for Z-fighting)
hud_pass=2     ; keep the HUD on top (Z-write); use hud_pass=1 if scene disappears
fps_boost=on   ; seems to work


[wiiu] ; guard line — stop old version reading <> as plain [] match
<residentevilcv><code veronica><codeveronica><veronica> ; Code Veronica
tex_cache=fast ; little bit better graphism
trans_sort=on ; displays menu
vertex_color=on ; highlight menu

[residentevilcv][code veronica][codeveronica][veronica] ; Code Veronica
tex_cache=very_fast+ ; fast also works good
vq_cmbr=on
trans_sort=on ; displays menu
vertex_color=on ; highlight menu

[residentevil2][resident evil 2] ; WinCE game, wont work
wince=yes

[residentevil3][resident evil 3]
tex_cache=very_fast ; very_fast works so yeah
adv_alpha=on ; needed for blend_mode
blend_mode=on ; per-polygon TSP blend needed for correct translucency


[Revolt][Re Volt][Re-Volt]
tex_cache=very_fast+ ; Very slow in menu otherwise
vq_cmpr=on ; fix green stuff in cache_very_fast/+
fixed_depth=0 ; don't put 2, it removes some polygons
hud_pass=2    ; needed to show intro screen. Maybe try hud_pass 1 ?
fastmem=off ; needed, otherwise crash when entering the race (or even before)
trans_sort=on ; needed to show elements in menu
vertex_color=on ; needed to see what's selected in menu
; split_screen=both; same as Quake3 didn't succed to have splitscreen working.

[roadsters]
tex_cache=very_fast+ ; fast/normal is supra slow at intro. Only very_fast/+ and quality works
vq_cmpr=on ; fix green stuff in cache_very_fast
ppz_write=off ; display menu writting correctly.
trans_sort=on ; fixes cars
audio_buffers=1 ; better audio, less fps
arm7_speed=1 ; gain speed

[sambadeamigo][amigo] ; samba de amigo
; controller=maracas ; untested
tex_cache=very_fast
trans_sort=on ; for menu to appear
fps_boost=on ; seems ok to use is

[segabassfishing][sega bass fishing]
controller=fishingrod
accuracy=balanced ;untested

[sega Extreme Sport][segaGT]
tex_cache=very_fast+
vq_cmbr=on


[sega GT][segaGT]
tex_cache=very_fast+ ; only way to have descent FPS (13fps instead of 4fps on intro demo)
vq_cmbr=on ; needed for very_fast/+
offset_color=off
split_screen=on ; needed for 2P mode (may get less FPS)

[sega worldwide soccer euro][sega worldwide soccer]
tex_cache=very_fast ; seems to work
fps_boost=on ; fix (a bit) menu for now
offset_color=on ; needed for field
rgb565_opaque_alpha=on ; needed for public to appear

[wiiu] ; guard line — stop old version reading <> as plain [] match
<segatetris><sega tetris> ; Wii U
accuracy=fast
adv_alpha=on
framebuffer_2d=on
fps_boost=off
graphics=normal
tex_cache=fast

[segatetris][sega tetris] ; Wii
accuracy=fast
adv_alpha=on
framebuffer_2d=on
fps_boost=on ; wrong alpha in scores but better FPS so yeah good tradeoff here
graphics=normal
tex_cache=fast


[wiiu] ; guard line — stop old version reading <> as plain [] match
<shadow man><shadow> ; Wii U
tex_cache=quality ; fast/normal works but menu trash
fps_boost=off ; safer

[shadow man][shadow]
tex_cache=quality ; fast/normal works but menu trash. Same with very_fast+ combined with vq_cmbr
fps_boost=on ; seems ok


[shenmue2][shenmue 2][shenmue II]
accuracy=fast ;
graphics=normal ; normal or low
tex_cache=fast ; very_fast is buggy
arm7_speed=1 ; improve speed

[shenmue] ; and What's Shenmue
accuracy=fast ;
graphics=normal ; normal or low
tex_cache=very_fast+ ; or fast
vq_cmbr=on ; needed with very_fast+ (remove if using fast)
arm7_speed=1 ; improve speed
fastmem=on    ; works !
bcache=on    ; One these 3 option avoid the "Ryo enter the house freeze/crash" in tex_cache=fast
fpu_pin=on   ; One these 3 option avoid the "Ryo enter the house freeze/crash" in tex_cache=fast
jit_align=on ; One these 3 option avoid the "Ryo enter the house freeze/crash" in tex_cache=fast
tmem_cache=on ; speed

[wiiu] ; guard line — stop old version reading <> as plain [] match
<snow surfers><snowsurfers>
tex_cache=fast ; fastest
trans_sort=on ; seems needed for some menu

[snow surfers][snowsurfers]
tex_cache=very_fast ; fastest

[silent scope][silentscope]
; Confirmed from an [SS] pass capture: ONE TA list is rendered twice per frame.
; First half = translucent extras + the scope disc (22 wedges fanning from screen
; centre at W=1.6, textured from the RTT target 554000) + the 24x-magnified world
; confined by a mode-2 user tile clip to the 288x288 corner. That is rendered into
; a 256x256 texture (FB_W_SOF1=01554000, XCLIP/YCLIP 0..255). Then the ordinary 1x
; world is appended and the WHOLE list is rendered to the display.
; keep does two things: the RTT render no longer consumes the list (so the display
; render still has the disc and the HUD), and the list is split by user tile clip —
; the mode-2 clipped strips are the texture's content, the unclipped ones are the
; on-screen overlay. Without the split the magnified half paints the screen's
; top-left corner and Z-fights the real scene.
; split_screen is NOT needed: keep reads the tile clip itself.
render_to_texture=keep ; CONFIRMED working (Dolphin) — untested on real Wii
;; Dolphin only: Graphics > Hacks > uncheck "Store EFB Copies to Texture Only"
;; and "Skip EFB Access from CPU", or the scope texture reads back empty (black)
tex_cache=fast ; very_fast is playable but trash in menu and text message
trans_sort=on ; needed, very visible when demo is playing
y_scaler=on ; needed for the scope to be 100% accurate

[soldier of fortune][soldieroffortune]

[sonicadventure2][sonic adventure 2] ; more specific first
tex_cache=very_fast+ ; works faster than "fast"
vq_cmbr=on ; needed with very_fast/+
; accuracy=accurate ; untested, needed ?
async_render=on ; 1 frame input latency
tmem_cache=on ; should be better
; fps_boost ; ok but some bad alpha
fixed_depth=2 ; mandatory, fixes Z-Fighting

[wiiu] ; guard line — stop old version reading <> as plain [] match
<sonicadventure><sonic adventure> ; Wii U
tex_cache=normal ; safer
fixed_depth=2 ; for Z-Fighting
; hud_pass=2 ; needed ?

[sonicadventure][sonic adventure]
tex_cache=fast ; very_fast works but janky
fixed_depth=2 ; for Z-Fighting
; hud_pass=2 ; needed ?

[soul calibur][soulcalibur] ; Soul Calibur
tex_cache=very_fast+ ; still janky but at least fast (70% atm). Fast/normal are way better but big FPS loss. Quality is trash both in performance and graphism
vq_cmbr=on ; needed with very_fast/+
yuv_stride=auto ; needed for character selection. Don't put texctl, it breaks gameplay
fixed_depth=2 ; fix menu
hud_pass=2 ; needed ?

[space channel 5 part 2]


[space channel 5][spacechannel5]
tex_cache=very_fast+ ; seems ok
vq_cmbr=on ; works with very_fast/+ . May improve FMV speed

[wiiu] ; guard line — stop old version reading <> as plain [] match
<soul fighter><soulfighter> ; Wii U
tex_cache=fast ; safer

[soul fighter][soulfighter]
tex_cache=very_fast ; very_fast works, some glitch in menu



[spawn]
tex_cache=very_fast+ ; fast/normal = menu buggy ? Test on real hardware
vq_cmbr=on ; needed with very_fast+
fixed_depth=2 ; needed fo fix z-fighting
hud_pass=2 ; needed for menu
trans_sort=on ; fix black alpha stuff . Off for more FPS (minor)

[spec ops 2][spec ops][specops]


[speed devils][speeddevils] ; Also called Speed Busters
tex_cache=fast ; very_fast works but a bit too janky
trans_sort=on ; fix car door missing
fps_boost=on ; works
split_screen=on ; 

[spider man][spider-man][spider]
tex_cache=fast ; very_fast is globally trash
fixed_depth=2 ; doesn't fix anything in dolphin
hud_pass=2 ; needed ? 
fps_boost=on ; seems ok to use
trans_sort=on ; untested, seems needed for black stuff that should be transparent

[Star Gladiator 2][stargladiator][Nightmare of Bilstein]
graphics=low ; 240p game

[star wars demolition][starwarsdemolition][star wars - demolition]
tex_cache=very_fast+ ; fast works too
vq_cmbr=on  ; needed with very_fast/+
ppz_write=off ; fixes menu
trans_sort=on ; needed for some stuff
split_screen=on ; needed for 2 players (can lower FPS)

[star wars - episode I - jedi power battle][jedi power battle]
tex_cache=normal ; seems good with that

[star wars - episode I - racer][star wars racer][starwarsracer]

[wiiu] ; guard line — stop old version reading <> as plain [] match
<streetfighter32><doubleimpact><double impact><sf32> ; Wii U
graphics=normal ; 480i mode
tex_cache=very_fast
blend_mode=off
canvas_width=384 ; SF3 draws its scene 384 wide (CPS3 arcade size) inside a 640 framebuffer
punch_through=on ; SF3 double impact stage background ?
trans_sort=on ; SF3 double impact stage background ?
async_render=off ; better performance if on but 1 frame input lag

[streetfighter32][doubleimpact][double impact][sf32]
graphics=low ; True 240p mode with L + Start
tex_cache=very_fast+
vq_cmbr=on
blend_mode=off
canvas_width=384 ; SF3 draws its scene 384 wide (CPS3 arcade size) inside a 640 framebuffer
punch_through=on ; SF3 double impact stage background ?
trans_sort=on ; SF3 double impact stage background ?
async_render=off ; better performance if on but 1 frame input lag

[wiiu] ; guard line — stop old version reading <> as plain [] match
<streetfighter33><3rdstrike><3rd strike><Street Fighter III - 3rd Strike><sf33> ; Wii U
graphics=normal
speed_limiter=off ; better ?
frameskip=1 ; may help FPS stability / performance
tex_cache=quality ; mandatory or crap
offset_color=off ; may help performance
tmem_cache=on ; may help performance
seam_fix=on ; needed in graphics=normal
jojo_fix=on ; fix bars (super move + life) ?
layer_sort=on ; fix background and pause menu ? (was hokuto_hack before 0.66)
async_render=off ; fix Remy stage ? Also no input lag

[streetfighter33][3rdstrike][3rd strike][Street Fighter III - 3rd Strike][sf33]
speed_limiter=off ; better ?
graphics=low ; L + Start to output 240p. Setting should be saved in-game for future boots
tex_cache=quality ; mandatory or crap
frameskip=1 ; may help FPS stability / performance
offset_color=off ; may help performance
async_render=on ; better performance if on but 1 frame input lag. (Remy stage buggy ?)
jojo_fix=on ; fix bars (super move + life) ?
layer_sort=on ; fix background and pause menu ? (was hokuto_hack before 0.66)
tmem_cache=on ; may help performance

[streetfighteralpha][street fighter alpha][sfa3] ; doesn't work right now
graphics=low ; 240p game

[super street fighter 2][super street fighter II][ssf2][matching service] ; SSF2 for matching service
graphics=low ; 240p game ? Needed anyway or slow
tex_cache=very_fast ; needed or very slow. Normal = slow, Fast = not tested


[super magnetic neo][supermagneticneo][magnetic]
tex_cache=very_fast+
vq_cmbr=on ; needed with very_fast/+
ppz_write=off ; fix remanence problem

<surf rocket racers>
tex_cache=normal ; safer probably. Not much performance difference with very_fast+

[surf rocket racers]
tex_cache=very_fast+ ; fast is trash. normal is ok
vq_cmbr=on ; needed with very_fast/+

[suzuki alstare extreme racing][alstare]
tex_cache=fast ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on)
trans_sort=on ; needed for some lights

[wiiu] ; guard line — stop old version reading <> as plain [] match
<sydney 2000><sydney>
tex_cache=normal ; safer
fixed_depth=2 ; needed for main gameplay or Z-fighting
hud_pass=0 ; don't use hud pass, it goes worse

[sydney 2000][sydney]
tex_cache=fast ; very_fast need to be re-tested (very_fast+ and vq_cmbr=on) 
fixed_depth=2 ; needed for main gameplay or Z-fighting
hud_pass=0 ; don't use hud pass, it goes worse



[tech romancer][techromancer] ; Tech Romancer
offset_color=off ; keep off, else white stuff

[testdrive][test drive] ; Test Drive 6
adv_alpha=on
tex_cache=normal ; very_fast+ and vq_cmbr works with minor bug. Maybe retest fast also ?
ppz_write=on ; needed for draw distance 
mipmap=off ; trilinear halves texture fill rate (-40% FPS) in this game


[theflinstones][the flinstones]
tex_cache=normal




[tokyo highway challenge 2]
tex_cache=normal ; only normal tested for now
offset_color=off ; specular off (else : white AE86)
fixed_depth=on ; fix a bit of z-fighting
hud_pass=2 ; probably needed, please test 1 also
decal_alpha=on ; else car is black & buggy
isp_cull=2 ; needed or big polygons blocking the view
punch_through=on ; needed or car lights on ground will be wrong, showing scenery

[tokyo highway challenge]
tex_cache=normal ; very_fast+ and vq_cmbr=on tested seems no FPS differences
offset_color=off ; specular off (else : white AE86)
split_screen=on ; for 2P multiplayer (can be slower on 1 player)

[tonyhawkproskater][tony hawk pro skater 2][thps2]
tex_cache=fast ; mandatory or can enter main gameplay. Very fast = Buggy
render_to_texture=on ; fix some weird black "barcode" bug + weird screen on top left

[tonyhawkproskater][tony hawk pro skater][thps]
tex_cache=normal ; fast works also with minor glitches
render_to_texture=on ; fix some weird black "barcode" bug + weird screen on top left
ppz_write=off ; fix Black remanence ?

[toy commander][toycommander] ; Toy Commander
tex_cache=fast ; very_fast is buggy.
fps_boost=on ; seems to be ok

[toy racer][toyracer] ; Toy Racer
tex_cache=fast ; very_fast is buggy.


[Twinkle Star Sprites][Twinkle Star][Twinkle]
graphics=low ; 240p game

[typingofthedead][typing of the dead]
controller=keyboard

[Uefa Dream Soccer] ; Infogrames
tex_cache=very_fast+
vq_cmbr=on
trans_sort=on ; needed ?

[Uefa Striker] ; Infogrames
tex_cache=fast ; very_fast+ is trash in menu
trans_sort=on ; needed ?
vtx_alpha=on ; needed for menu
isp_cull=0 ; 2 didn't change. 1 untested

[unreal] ; Unreal Tournament
tex_cache=very_fast+
vq_cmbr=on
fixed_depth=2 ; slow down menu but fix black flickering ingame
ppz_write=off ; needed to fix black Remanance
split_screen=multipass ; not working for now. Experimental branch : quake3-splitscreen

[virtuacop] ; WinCE Game
wince=yes
controller=lightgun


[wacky][whacky] ; Wacky Races (alt spelling — "wacky" is NOT a substring of "whacky")
tex_cache=very_fast+ ; normal works for sure
vq_cmbr=on ; needed with very_fast/+
x_scaler=on ; renders 1280 wide with the PVR X scaler; without this only the left half shows




[V-Rally][vrally] ; V-Rally 2
tex_cache=quality ; only way to display menu
ppz_write=off ; to fix remanence ingame
; fps_boost=on ; try it later when alpha/transparency will be fixed

[vigilante 8][vigilante8]



[virtua striker] ; Virtua Striker 2
tex_cache=fast
; depth_clip=off ; Dolphin. Maybe real hardware ?

[virtua fighter] ; Virtua Fighter 3tb
tex_cache=fast ; very_fast/+ with vq_cmbr=on works also but it seems no fps boost
fps_boost=on ; no visual glitches seen
yuv_twiddle_fix=on ; "FIRST MATCH" loading art is green without it

[wwf attitude]
tex_cache=fast ; haven't tested very_fast
x_scaler=on ; seems needed, same problem as Omicron/Whacky racer otherwise
trans_sort=on 
rgb565_opaque_alpha=on ; mandatory


[wwf royal rumble]
tex_cache=normal ; minor bugs. Can use fast also
offset_color=off ; white characters otherwise
fps_boost=off    ; tested : character selection is trash if on


[Walt disney Magical Racing rour][Magical Racing rour]
tex_cache=very_fast+ ; fast ok also
vq_cmbr=on ; needed with very_fast/+
split_screen=multipass ; 2P splitscreen flicker

[wetrix]
tex_cache=very_fast ; works !
trans_sort=on       ; for water in intro
offset_color=off    ; white board in gameplay otherwise

[Yu Suzuki Game Works]
graphics=low ; 240p game

[Zero Gunner 2][Zero Gunner]
tex_cache=very_fast ; works !
fixed_depth=2       ; TIGHT
hud_pass=2          ; PROTECT (Z-WRITE), pairs with fixed_depth=2

[wiiu] ; guard line — stop old version reading <> as plain [] match
<zombierevenge><zombie revenge> ; Wii U
tex_cache=normal ; safer
offset_color=off ; safer
fixed_depth=2 ; TIGHT
hud_pass=2    ; PROTECT (Z-WRITE), pairs with fixed_depth=2

[zombierevenge][zombie revenge]
tex_cache=fast ; seems ok
; fps_boost=on ; improve FPS a bit (not worse it)
offset_color=off ; safer
fixed_depth=2 ; TIGHT
hud_pass=2    ; PROTECT (Z-WRITE), pairs with fixed_depth=2

;; ---------------------------------------------------------------------------
;; WinCE games — need the Windows CE syscall layer, which NullDC4Wii does not
;; emulate. wince=yes shows a warning after the options menu (A: launch
;; anyway, B: back to file list) instead of silently failing. See
;; compatibility.txt for the per-game symptom (boot to BIOS, white/black
;; screen, freeze). No other fields set — there is nothing to tune.
;; ---------------------------------------------------------------------------

[4x4 evolution][4x4evolution]
wince=yes

[armada]
wince=yes

[atari anniversary][atarianniversary]
wince=yes

[border down] ; WinCE-like crash
wince=yes

[gunship elite][gunshipelite] ; Bang! Gunship Elite
wince=yes

[bust a move][bustamove] ; Bust A Move4
wince=yes

[caesars palace][caesarspalace]
wince=yes

[championship surfer][champsurfer]
wince=yes

[densha de go 2][denshadego2]
wince=yes

[ducati world][ducati]
wince=yes

[half life][halflife][half-life] ; homebrew Xash3D port, see compatibility.txt
wince=yes

[lovely fruit park][fruitpark] ; Hello Kitty: Lovely Fruit Park
wince=yes

[hello kitty cookie][kitty cookie] ; Hello Kitty: Cookie
wince=yes

[hidden and dangerous][hiddendangerous]
wince=yes

[hoyle casino][hoylecasino]
wince=yes

[jimmy white][jimmywhite] ; Jimmy White's 2: Cueball
wince=yes

[psycho circus][psychocircus] ; KISS Psycho Circus: The Nightmare Child
wince=yes

[maximum pool][maximumpool]
wince=yes

[arcade hits vol 1][arcadehits1] ; Midway's Greatest Arcade Hits Vol. 1
wince=yes

[arcade hits vol 2][arcadehits2] ; Midway's Greatest Arcade Hits Vol. 2
wince=yes

[quarterback club][quarterbackclub] ; NFL Quarterback Club 2001
wince=yes

[nightmare creatures ii][nightmare creatures 2] ; NOT plain "Nightmare Creatures" (non-WinCE, unrelated game)
wince=yes

[qbert]
wince=yes

[quake proto][quakeproto] ; Quake (prototype) — NOT quake3/Quake III Arena
wince=yes

[railroad tycoon][railroadtycoon]
wince=yes

[rainbow six][rainbowsix][tom clancy] ; Rainbow Six / Tom Clancy's Rainbow Six
wince=yes

[sega rally 2][segarally2] ; NOT plain "sega rally" (keep specific to 2)
wince=yes

[sno-cross][snocross]
wince=yes

[luv shack][luvshack] ; South Park: Chef's Luv Shack
wince=yes

[spirit of speed][spiritofspeed]
wince=yes

[starlancer]
wince=yes

[super runabout][runabout] ; Super Runabout: San Francisco Edition
wince=yes

[taxi 2][taxi2] ; Luc Besson's Taxi 2 movie tie-in, NOT Crazy Taxi 2
wince=yes

[next tetris][nexttetris]
wince=yes

[typing of the date][typingofthedate] ; NOT typingofthedead (different, working game)
wince=yes

[tomb raider chronicles][raiderchronicles]
wince=yes

[last revelation][lastrevelation] ; Tomb Raider: The Last Revelation
wince=yes

[urban chaos][urbanchaos]
wince=yes

[wild metal][wildmetal]
wince=yes

[worms armageddon][wormsarmageddon]
wince=yes

[worms world party][wormsworldparty]
wince=yes

